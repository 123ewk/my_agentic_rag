# Agentic RAG 项目开发流程文档

> **项目概述**: 基于 LangGraph 状态机的 Agentic RAG 智能知识库系统，支持多意图识别、多策略检索、流式输出、长短记忆和反思优化。

---

## 📋 目录

1. [项目初始化与环境配置](#1-项目初始化与环境配置)
2. [配置管理系统开发](#2-配置管理系统开发)
3. [日志系统开发](#3-日志系统开发)
4. [文档处理模块开发](#4-文档处理模块开发)
5. [向量数据库模块开发](#5-向量数据库模块开发)
6. [检索与重排模块开发](#6-检索与重排模块开发)
7. [记忆系统开发](#7-记忆系统开发)
8. [工具模块开发](#8-工具模块开发)
9. [Agent 核心状态机开发](#9-agent-核心状态机开发)
10. [API 服务层开发](#10-api-服务层开发)
11. [测试体系开发](#11-测试体系开发)
12. [主程序集成与启动](#12-主程序集成与启动)
13. [定时任务模块开发](#13-定时任务模块开发)

---

## 1. 项目初始化与环境配置

### 1.1 项目依赖管理

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\pyproject.toml`

```toml
[project]
name = "agentic-rag"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.2.15",
    "langgraph>=1.1.6",
    "fastapi>=0.136.0",
    "uvicorn>=0.44.0",
    "pymilvus==2.6.10",
    ...
]
```

**📍 环境变量模板**: `d:\LangChain\实习项目\Agentic_RAG\.env.example`

```env
# 大模型 API 配置
QWEN_API_KEY=your_key_here
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
ZHIPUAI_API_KEY=your_key_here
MINIMAX_API_KEY=your_key_here

# 数据库配置
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5433/postgres

# API 安全配置
API_KEY=your_api_key_here
```

---

## 2. 配置管理系统开发

### 2.1 全局配置类

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\config\settings.py`

```python
class Settings(BaseSettings):
    """全局配置类"""
    # LLM 配置
    llm_name: str = "Qwen3.5-plus"
    llm_temperature: float = 0.7
    qwen_api_key: str = os.getenv("QWEN_API_KEY", "")
    
    # Milvus 配置
    milvus_host: str = "localhost"
    milvus_port: int = 19530
    milvus_collection: str = "agentic_rag"
    
    # 数据库配置
    database_url: str = os.getenv("DATABASE_URL", "...")
    
    # 检索/记忆/缓存等配置
    retrieval_top_k: int = 5
    rerank_top_k: int = 3
    short_term_memory_k: int = 10
    intent_cache_enabled: bool = True
    generation_cache_enabled: bool = True
    
    @lru_cache()
    def get_settings() -> Settings:
        """获取配置实例（单例模式）"""
        return Settings()
```

**功能说明**:
- 使用 `pydantic-settings` 实现类型安全的配置管理
- 自动从 `.env` 文件和环境变量加载配置
- 使用 `@lru_cache()` 实现单例缓存，避免重复初始化

---

## 3. 日志系统开发

### 3.1 日志配置

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\config\logger_config.py`

```python
from loguru import logger

def setup_logging():
    """配置日志系统"""
    # 设置日志级别、格式、输出目标
    ...
```

---

## 4. 文档处理模块开发

### 4.1 多格式文档加载器

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\document_processing\loaders.py`

```python
class DocumentLoader:
    """统一文档加载器"""
    
    def __init__(self):
        self.loaders = {
            '.pdf': PDFLoader(),
            '.docx': WordLoader(),
            '.md': MarkdownLoader(),
            '.txt': MarkdownLoader(),
            '.csv': ExcelCSVLoader(),
            '.xlsx': ExcelCSVLoader(),
            '.html': WebLoader(),
            ...
        }
```

**支持的格式**:
| 格式 | 加载器类 | 功能 |
|------|----------|------|
| PDF  | `PDFLoader` | 按页提取文本和元数据 |
| Word | `WordLoader` | 按段落提取文本 |
| Markdown/Text | `MarkdownLoader` | 解析 Front Matter |
| Excel/CSV | `ExcelCSVLoader` | 多 Sheet 处理，转为表格文本 |
| HTML | `WebLoader` | 爬取网页，清洗 script/style 标签 |

### 4.2 文档分块策略

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\document_processing\splitters.py`

```python
class SemanticChunker(TextSplitter):
    """语义分块器（基于句子边界）"""
    
    def _split_by_paragraph_and_sentence(self, doc):
        """先分段 → 再分句 → 再合并（最稳定的方案）"""
        ...
    
    def _merge_chunks(self, segments):
        """合并分段为块，支持滑动窗口重叠（overlap）"""
        ...
```

**三种分块策略**:
- **RecursiveChunker**: 递归字符分块，按 `["\n\n", "\n", "。", "!", "?"]` 优先级分割
- **SemanticChunker**: 语义分块，基于 Spacy 的句子边界检测
- **AdaptiveChunker**: 自适应分块，根据文档类型动态调整策略

---

## 5. 向量数据库模块开发

### 5.1 Milvus 向量数据库封装

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\vectorstore\milvus_client.py`

```python
class MilvusClient:
    """Milvus 向量数据库封装"""
    
    def __init__(self, collection_name, embedding_model, connection_args):
        ...
    
    def _normalize_metadata(self, documents):
        """统一元数据字段，防止 Milvus schema 不匹配"""
        ...
    
    def from_documents(self, documents, drop_old=False):
        """从文档创建向量存储"""
        ...
    
    def add_documents(self, documents):
        """添加文档，自动处理 schema 重建"""
        ...
    
    def similarity_search(self, query, k=5):
        """相似度搜索"""
        ...
```

### 5.2 嵌入模型初始化

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\vectorstore\embeddings.py`

```python
def get_embeddings(embedding_type="zhipu", api_key, model_name, dimension):
    """获取嵌入模型实例"""
    ...
```

---

## 6. 检索与重排模块开发

### 6.1 查询改写器

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\retrieval\query_rewrite.py`

```python
class QueryRewriter:
    """查询重写器（综合）"""
    
    def __init__(self, llm, embeddings):
        self.expansion = QueryExpansion(llm)    # 查询扩展
        self.hyde = HyDE(llm, embeddings)        # 假设性文档嵌入
        self.decomposer = QueryDecomposer(llm)   # 查询分解
    
    def rewrite(self, query, strategy="all"):
        """支持 expansion / hyde / decomposition / all 四种策略"""
        ...
```

### 6.2 混合检索器

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\retrieval\hybrid_search.py`

```python
class HybridSearchRetriever:
    """混合检索器：向量检索 + BM25 关键词检索"""
    
    def __init__(self, vector_retriever, embeddings, vector_weight=0.6, keyword_weight=0.4):
        ...
    
    def invoke(self, query, k=5):
        """加权融合两种检索结果"""
        ...
```

### 6.3 重排模型

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\retrieval\reranker.py`

```python
class BGGReranker(Reranker):
    """BGE-reranker 重排器"""
    
    def rerank(self, query, documents, top_k=3):
        """调用 BGE rerank API，按相关性重新排序"""
        ...
```

---

## 7. 记忆系统开发

### 7.1 短期记忆（PostgreSQL）

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\memory\short_term.py`

```python
class ShortTermMemory:
    """短期记忆管理器（PostgreSQL 持久化）"""
    
    @retry(stop=stop_after_attempt(5), wait=wait_exponential(...))
    async def connect(self):
        """带自动重试的异步数据库连接"""
        ...
    
    async def add_message(self, session_id, question, answer, metadata):
        """添加对话消息"""
        ...
    
    async def get_message(self, session_id, limit):
        """获取历史消息"""
        ...
    
    async def get_context(self, session_id, max_tokens):
        """基于 token 限制的智能截断"""
        ...
```

**核心特性**:
- 异步连接池（pool_size=10, max_overflow=20）
- 带重试机制的稳健连接
- 基于 token 数量的上下文截断
- 24 小时 TTL 自动过期清理

### 7.2 长期记忆（PostgreSQL + pgvector）

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\memory\long_term.py`

```python
class LongTermMemory:
    """长期记忆管理器（PostgreSQL + pgvector）"""
    
    async def save_memory(self, user_id, content, session_id, metadata):
        """保存记忆（生成向量 + 插入）"""
        ...
    
    async def search(self, user_id, query, k, metadata_filter):
        """向量相似度搜索，支持元数据过滤"""
        ...
    
    async def save_batch(self, user_id, contents, ...):
        """批量保存，一次性生成所有嵌入（快 3-5 倍）"""
        ...
```

### 7.3 缓存系统

| 缓存类型 | 代码位置 | 功能 |
|----------|----------|------|
| 意图缓存 | `agentic_rag/memory/intent_cache.py` | LRU 内存缓存意图识别结果，减少重复 LLM 调用 |
| 生成缓存 | `agentic_rag/memory/gen_cache.py` | 缓存完整回答，相同问题直接返回 |
| LLM 缓存 | `agentic_rag/memory/llm_cache.py` | LLM 调用级别缓存 |

---

## 8. 工具模块开发

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\tools\search.py`

```python
@tool
def duckduckgo_search(query: str) -> str:
    """使用 DuckDuckGo 搜索互联网信息"""
    ...

@tool
def calculator(expression: str) -> str:
    """安全计算器，只允许数字和运算符"""
    ...

@tool
def python_repl(code: str) -> str:
    """Python 代码执行器（沙箱环境），禁用 __builtins__"""
    ...
```

**工具字典获取**:
```python
def get_search_tools() -> Dict[str, Any]:
    return {
        "duckduckgo_search": duckduckgo_search,
        "calculator": calculator,
        "python_repl": python_repl
    }
```

---

## 9. Agent 核心状态机开发

这是整个系统最核心的部分，基于 LangGraph 构建。

### 9.1 状态定义

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\agent\state.py`

```python
class AgentState(TypedDict):
    """Agent 状态定义"""
    question: str              # 用户问题
    intent: str                # 意图：factual / reasoning / summary / multi_hop
    rewritten_queries: List[str]
    retrieved_docs: List[Document]
    reranked_docs: List[Document]
    generation: str
    refined_answer: str
    evaluation: Dict[str, float]
    needs_reflection: bool
    tool_results: Dict[str, Any]
    memory_context: List[str]
    conversation_history: List[Dict]
    reflection_count: int
    error: Optional[str]
    metadata: Dict[str, Any]
```

### 9.2 节点实现

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\agent\nodes.py`

| 节点函数 | 功能 |
|----------|------|
| `intent_classification_node` | 意图分类，带缓存优化 |
| `query_rewrite_node` | 查询改写 |
| `retrieval_node` | 单查询检索 |
| `parallel_retrieval_node` | 并行检索（性能优化版） |
| `rerank_node` | 文档重排 |
| `tool_call_node` | 工具调用 |
| `generation_node` | 回答生成，支持记忆上下文和上下文截断 |
| `evaluation_node` | 评估回答质量 |
| `reflection_node` | 反思优化 |

### 9.3 边与路由逻辑

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\agent\edges.py`

```
intent_classification → {tool_call, query_rewrite, generation}
query_rewrite → {retrieval, rerank}
rerank → {tool_call, generation}
tool_call → generation
generation → evaluation
evaluation → {reflection, __end__}
reflection → {evaluation, __end__}
```

### 9.4 图构建与执行

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\agent\graph.py`

```python
class AgenticRAGGraph:
    """Agentic RAG 状态机"""
    
    def _build_graph(self):
        """构建状态机图"""
        graph = StateGraph(AgentState)
        graph.add_node("intent_classification", ...)
        graph.add_node("query_rewrite", ...)
        ...
        graph.add_conditional_edges("intent_classification", route_after_intent, {...})
        ...
        return graph.compile()
    
    def invoke(self, question, **kwargs):
        """同步执行 Agent"""
        ...
    
    async def stream_invoke(self, question, ...):
        """流式执行，实时返回状态和生成内容"""
        ...
```

**执行流程图解**:
```
用户问题
   ↓
[意图识别] ──(factual)──→ [直接检索] ──→ [重排/生成]
   │
   ├─(reasoning/multi_hop)──→ [查询改写] ──→ [并行检索] ──→ [重排]
   │
   └─(summary)──→ [直接生成]
   
   ↓
   [工具调用]（如果需要）
   ↓
   [回答生成]
   ↓
   [评估] ──(质量低)──→ [反思] ──→ [再评估]
   │
   └─(质量高)──→ 返回结果
```

---

## 10. API 服务层开发

### 10.1 数据模型（Schema）

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\api\schemas.py`

```python
class QueryRequest(BaseModel):
    question: str = Field(..., min_length=1, max_length=2000)
    session_id: Optional[str] = None
    user_id: Optional[str] = None
    model_name: Optional[str] = None
    use_tools: bool = True
    use_fast_path: bool = False
    max_reflection: int = Field(2, ge=0, le=5)
    temperature: float = Field(0.7, ge=0.0, le=2.0)

class QueryResponse(BaseModel):
    answer: str
    sources: List[SourceDocument]
    metrics: Dict[str, float]
    session_id: str
    intent: str
    tools_used: List[str]
    reflection_count: int
    processing_time: float
    ...
```

### 10.2 路由实现

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\api\routes.py`

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 查询接口 | POST | `/api/v1/query` | RAG 查询（同步） |
| 流式查询 | POST | `/api/v1/query/stream` | RAG 查询（SSE 流式） |
| 文档上传 | POST | `/api/v1/upload` | 多格式文档上传并索引 |
| 健康检查 | GET | `/api/v1/health` | 组件健康状态检测 |

**生产级特性**:
```python
# 1. 生命周期管理
@asynccontextmanager
async def lifespan(app: FastAPI):
    # 启动时初始化 Agent
    _agent_instance = create_agent()
    yield
    # 关闭时清理资源
    _agent_instance = None

# 2. API Key 认证
async def verify_api_key(request: Request):
    api_key = request.headers.get("X-API-Key")
    ...

# 3. 请求限流
class RateLimiter:
    """滑动窗口限流：60次/60秒"""
    ...

# 4. 请求追踪中间件
@app.middleware("http")
async def add_process_time_header(request, call_next):
    request_id = str(uuid.uuid4())
    ...
    response.headers["X-Request-ID"] = request_id
    response.headers["X-Process-Time"] = str(process_time)
    ...

# 5. 全局异常处理
@app.exception_handler(Exception)
async def global_exception_handler(request, exc):
    ...

# 6. CORS 配置
app.add_middleware(CORSMiddleware, allow_origins=[...], ...)

# 7. 流式响应
@app.post("/api/v1/query/stream")
async def query_stream(request, ...):
    return StreamingResponse(
        generate_stream_response(...),
        media_type="text/event-stream"
    )
```

---

## 11. 测试体系开发

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\tests\`

| 测试文件 | 测试内容 |
|----------|----------|
| `test_nodes.py` | 各节点逻辑测试 |
| `test_edges.py` | 路由逻辑测试 |
| `test_state.py` | 状态定义测试 |
| `test_retrieval.py` | 检索模块测试 |
| `test_reranker.py` | 重排模块测试 |
| `test_memory.py` | 记忆模块测试 |
| `test_evaluation.py` | 评估模块测试 |
| `test_tools.py` | 工具模块测试 |
| `test_backend_api.py` | 后端 API 测试 |
| `test_frontend_automation.py` | 前端自动化测试 |
| `test_stream_api.py` | 流式 API 测试 |
| `test_integration.py` | 集成测试 |
| `conftest.py` | 测试夹具和共享配置 |

---

## 12. 主程序集成与启动

### 12.1 入口程序

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\main.py`

```python
def initialize_components():
    """初始化所有组件"""
    # 1. 初始化数据库表
    init_memory_tables_sync(settings.database_url)
    
    # 2. 初始化嵌入模型
    embeddings = get_embeddings(...)
    
    # 3. 初始化向量存储
    vectorstore = MilvusClient(...)
    
    # 4. 初始化重排模型
    reranker = get_reranker(...)
    
    # 5. 初始化 LLM
    llm = ChatOpenAI(...)
    
    # 6. 初始化短期记忆
    short_term_memory = ShortTermMemory(...)
    
    # 7. 初始化长期记忆
    long_term_memory = LongTermMemory(...)
    
    # 8. 定义提示词模板
    prompt_template = "..."
    
    # 9. 初始化工具
    tools = {"duckduckgo": duckduckgo_search, "calculator": calculator}
    
    # 10. 初始化 Agent
    agent = AgenticRAGGraph(
        llm, embeddings, vectorstore, reranker, tools,
        prompt_template, short_term_memory, long_term_memory
    )
    
    return {...}

def main():
    """主函数"""
    components = initialize_components()
    app = create_app()
    uvicorn.run(app, host="127.0.0.1", port=settings.api_port)

if __name__ == "__main__":
    main()
```

### 12.2 启动命令

```bash
# 1. 安装依赖
uv sync

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env 填入 API Key

# 3. 启动服务
python main.py
# 或
uv run python main.py

# 4. 访问 API 文档
# http://127.0.0.1:8000/api/docs
```

---

## 13. 定时任务模块开发

### 13.1 定时任务调度器架构

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\schedulers\`

系统提供基于 APScheduler 的定时任务调度器，用于自动执行系统维护任务。采用单例模式管理调度器，确保全局只有一个调度器实例在运行。

**模块文件**:
| 文件 | 功能 |
|------|------|
| `__init__.py` | 模块导出，统一提供调度器访问接口 |
| `long_scheduler.py` | 定时任务调度器（包含短期和长期记忆清理） |

**核心类**: `TaskScheduler`

```python
class TaskScheduler:
    """定时任务调度器单例类"""
    
    _instance = None
    _scheduler = None
    
    def __new__(cls):
        """单例模式实现"""
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance
    
    def __init__(self):
        if self._scheduler is None:
            self._scheduler = BackgroundScheduler(timezone="Asia/Shanghai")
    
    def start(self):
        """启动调度器"""
        ...
    
    def shutdown(self):
        """停止调度器"""
        ...
```

### 13.2 短期记忆清理任务

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\schedulers\long_scheduler.py`

负责清理短期记忆中已过期的会话数据，避免数据库积累过多无用数据。

**执行时间**: 每天凌晨 2:00（Asia/Shanghai）

```python
def add_cleanup_expired_task(self, short_term_memory):
    """
    添加清理过期会话的定时任务
    
    Args:
        short_term_memory: ShortTermMemory实例
    """
    def cleanup_task():
        """清理过期会话的任务"""
        try:
            logger.info("开始执行清理过期会话任务...")
            loop = asyncio.new_event_loop()
            asyncio.set_event_loop(loop)
            try:
                deleted_count = loop.run_until_complete(short_term_memory.cleanup_expired())
                logger.info(f"清理过期会话任务完成，删除了 {deleted_count} 条记录")
            finally:
                loop.close()
        except Exception as e:
            logger.error(f"清理过期会话任务执行失败：{e}")
    
    self._scheduler.add_job(
        cleanup_task,
        CronTrigger(hour=2, minute=0, timezone="Asia/Shanghai"),
        id="cleanup_expired_sessions",
        name="清理过期会话",
        replace_existing=True
    )
```

**任务特点**:
- 使用 `CronTrigger` 按 Cron 表达式调度
- 自动创建新的事件循环处理异步任务
- 完善的异常捕获和日志记录
- 使用 `replace_existing=True` 确保任务唯一性

### 13.3 长期记忆清理任务

**📍 代码位置**: `d:\LangChain\实习项目\Agentic_RAG\agentic_rag\schedulers\long_scheduler.py`

负责清理长期记忆中的旧数据和重复数据，保持记忆库的高效性。

**执行时间**: 每天凌晨 3:30（Asia/Shanghai）

```python
def add_cleanup_old_memories_task(self, long_term_memory, user_ids: list = None, retention_days: int = 90):
    """
    添加清理长期记忆的定时任务（按时间清理旧记忆 + 清理重复记忆）
    
    Args:
        long_term_memory: LongTermMemory实例
        user_ids: 需要清理的用户ID列表（None表示运行时动态获取）
        retention_days: 记忆保留天数，默认90天
    """
    def cleanup_task():
        """清理旧长期记忆的任务"""
        try:
            logger.info("开始执行清理长期记忆任务...")
            
            loop = asyncio.new_event_loop()
            asyncio.set_event_loop(loop)
            try:
                async def run_cleanup():
                    actual_user_ids = user_ids
                    if actual_user_ids is None:
                        actual_user_ids = await long_term_memory.get_all_user_ids()
                    
                    if actual_user_ids:
                        total_deleted = 0
                        for user_id in actual_user_ids:
                            deleted_count = await long_term_memory.cleanup_old_memories(user_id, retention_days)
                            total_deleted += deleted_count
                            logger.info(f"用户 {user_id} 清理了 {deleted_count} 条旧记忆")
                        
                        dup_deleted = 0
                        for user_id in actual_user_ids:
                            dup_count = await long_term_memory.cleanup_duplicates(user_id)
                            dup_deleted += dup_count
                            logger.info(f"用户 {user_id} 清理了 {dup_count} 条重复记忆")
                        
                        logger.info(f"长期记忆清理任务完成，共删除 {total_deleted} 条旧记忆，{dup_deleted} 条重复记忆")
                    else:
                        logger.info("没有找到有长期记忆的用户，跳过清理")
                
                loop.run_until_complete(run_cleanup())
            finally:
                loop.close()
                
        except Exception as e:
            logger.error(f"清理长期记忆任务执行失败：{e}")
    
    self._scheduler.add_job(
        cleanup_task,
        CronTrigger(hour=3, minute=30, timezone="Asia/Shanghai"),
        id="cleanup_old_memories",
        name="清理旧长期记忆",
        replace_existing=True
    )
```

**清理内容**:
| 清理类型 | 说明 | 默认保留天数 |
|----------|------|--------------|
| 旧记忆清理 | 删除超过指定天数的长期记忆 | 90 天 |
| 重复记忆清理 | 删除相同用户的重复记忆记录 | - |

### 13.4 调度器使用方式

```python
from agentic_rag.schedulers import get_scheduler

# 获取调度器单例
scheduler = get_scheduler()

# 添加短期记忆清理任务
scheduler.add_cleanup_expired_task(short_term_memory)

# 添加长期记忆清理任务
scheduler.add_cleanup_old_memories_task(
    long_term_memory,
    user_ids=None,  # None表示运行时动态获取所有用户
    retention_days=90
)

# 启动调度器
scheduler.start()

# 在应用关闭时停止调度器
scheduler.shutdown()
```

**定时任务汇总**:
| 任务名称 | 执行时间 | 清理内容 |
|----------|----------|----------|
| 清理过期会话 | 每天 02:00 | 短期记忆中超过 24 小时的会话 |
| 清理旧长期记忆 | 每天 03:30 | 超过 90 天的长期记忆 + 重复记忆 |

---

## 📁 完整项目结构

```
d:\LangChain\实习项目\Agentic_RAG\
├── main.py                              # 🚀 主程序入口
├── pyproject.toml                       # 📦 依赖管理
├── .env.example                         # 🔑 环境变量模板
├── .gitignore                           # 🚫 Git 忽略规则
│
└── agentic_rag/
    ├── config/
    │   ├── settings.py                  # ⚙️ 全局配置
    │   └── logger_config.py             # 📝 日志配置
    │
    ├── document_processing/
    │   ├── loaders.py                   # 📄 多格式文档加载器
    │   └── splitters.py                 # ✂️ 文档分块策略
    │
    ├── vectorstore/
    │   ├── milvus_client.py             # 💾 Milvus 向量数据库封装
    │   └── embeddings.py                # 🔢 嵌入模型初始化
    │
    ├── retrieval/
    │   ├── query_rewrite.py             # 🔍 查询改写（扩展/分解/HyDE）
    │   ├── hybrid_search.py             # 🔎 混合检索（向量 + BM25）
    │   └── reranker.py                  # 📊 文档重排
    │
    ├── memory/
    │   ├── short_term.py                # 🧠 短期记忆（PostgreSQL）
    │   ├── long_term.py                 # 🧠 长期记忆（PostgreSQL + pgvector）
    │   ├── cache.py                     # 🗄️ 缓存基类
    │   ├── intent_cache.py              # 💡 意图缓存
    │   ├── gen_cache.py                 # 💡 生成缓存
    │   └── llm_cache.py                 # 💡 LLM 调用缓存
    │
    ├── tools/
    │   ├── search.py                    # 🛠️ 搜索/计算器/代码执行工具
    │   └── tool_calls.py                # 🔄 工具调用逻辑
    │
    ├── agent/
    │   ├── state.py                     # 📋 Agent 状态定义
    │   ├── nodes.py                     # 🔗 节点实现（9个处理节点）
    │   ├── edges.py                     # ➡️ 边与路由逻辑（7个路由函数）
    │   ├── graph.py                     # 📊 图构建与执行（同步/流式）
    │   └── base_rag.py                  # 🏗️ RAG 基类
    │
    ├── evaluation/
    │   └── metrics.py                   # 📈 评估指标
    │
    ├── models/
    │   ├── short_term_model.py          # 🗃️ 短期记忆 ORM 模型
    │   └── long_term_model.py           # 🗃️ 长期记忆 ORM 模型
    │
    ├── api/
    │   ├── schemas.py                   # 📝 API 数据模型
    │   ├── routes.py                    # 🌐 API 路由实现
    │   └── db_init.py                   # 🗄️ 数据库初始化
    │
    ├── lock/
    │   ├── redis_lock.py                # 🔒 Redis 分布式锁
    │   └── postgresql_lock.py           # 🔒 PostgreSQL 锁
    │
    ├── schedulers/
    │   ├── __init__.py                  # 📅 调度器模块导出
    │   └── long_scheduler.py            # 📅 定时任务调度器（短期+长期记忆清理）
    │
    ├── ui/
    │   └── app.py                       # 🎨 Streamlit 前端界面
    │
    └── tests/                           # 🧪 测试套件（13个测试文件）
        ├── conftest.py
        ├── test_nodes.py
        ├── test_edges.py
        ├── test_state.py
        ├── test_retrieval.py
        ├── test_reranker.py
        ├── test_memory.py
        ├── test_evaluation.py
        ├── test_tools.py
        ├── test_backend_api.py
        ├── test_frontend_automation.py
        ├── test_stream_api.py
        └── test_integration.py
```

---

## 🔧 核心架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                          FastAPI API 层                              │
│  POST /api/v1/query  │  POST /api/v1/query/stream  │  POST /api/v1/upload │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────────────┐
│                      AgenticRAGGraph（状态机）                        │
│                                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │ 意图识别节点  │→│ 查询改写节点  │→│ 并行检索节点  │              │
│  │ (带缓存)     │  │ (4种策略)    │  │ (线程池)     │              │
│  └──────────────┘  └──────────────┘  └──────┬───────┘              │
│                                              │                      │
│                    ┌─────────────────────────▼─┐                    │
│                    │     重排节点               │                    │
│                    │  (BGE-reranker API)       │                    │
│                    └──────────────┬────────────┘                    │
│                                 │                                  │
│                    ┌────────────▼────────────┐                     │
│                    │     工具调用节点          │                     │
│                    │ (搜索/计算器/代码执行)    │                     │
│                    └────────────┬────────────┘                     │
│                                 │                                  │
│                    ┌────────────▼────────────┐                     │
│                    │     生成节点              │                     │
│                    │ (记忆+对话历史+上下文截断) │                     │
│                    └────────────┬────────────┘                     │
│                                 │                                  │
│                    ┌────────────▼────────────┐                     │
│                    │     评估节点              │                     │
│                    │ (轻量级，不调用LLM)       │                     │
│                    └────────────┬────────────┘                     │
│                                 │                                  │
│                    ┌────────────▼────────────┐                     │
│                    │     反思节点              │                     │
│                    │ (最多2次循环)             │                     │
│                    └─────────────────────────┘                     │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────────────┐
│                         基础设施层                                    │
│                                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │  Milvus      │  │ PostgreSQL   │  │  Redis       │              │
│  │  向量数据库   │  │ (短期记忆)   │  │ (分布式锁)   │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│                                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │  LLM API     │  │ Embedding    │  │  Reranker    │              │
│  │ (Qwen/MiniMax)│  │ API (智谱)   │  │  API (智谱)  │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │              APScheduler 定时任务调度器                       │   │
│  │  • 02:00 清理短期记忆过期会话  • 03:30 清理长期记忆旧数据       │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📊 数据流

```
用户请求 → API 验证（API Key / 限流）→ 意图分类（缓存命中？）
   │
   ├── factual（事实查询）→ 直接使用原问题检索 → 重排/生成 → 返回
   │
   ├── reasoning/multi_hop（推理/多跳）→ 查询改写 → 并行检索 → 重排 → 生成 → 返回
   │
   └── summary（摘要）→ 直接生成 → 返回

生成后 → 评估质量 → 需要反思？→ 是 → 反思优化 → 再评估（最多2次）
                                  → 否 → 保存短期记忆 → 返回最终结果
```

---

## 🎯 关键特性总结

| 特性 | 实现方式 | 代码位置 |
|------|----------|----------|
| 多意图识别 | factual / reasoning / summary / multi_hop | `agent/nodes.py` |
| 查询改写 | 4种策略：扩展 / HyDE / 分解 / 综合 | `retrieval/query_rewrite.py` |
| 并行检索 | ThreadPoolExecutor 并行检索 | `agent/nodes.py` |
| 混合检索 | 向量 + BM25 关键词 | `retrieval/hybrid_search.py` |
| 文档重排 | BGE-reranker API | `retrieval/reranker.py` |
| 流式输出 | SSE Server-Sent Events | `api/routes.py` |
| 短期记忆 | PostgreSQL 异步连接池 | `memory/short_term.py` |
| 长期记忆 | PostgreSQL + pgvector | `memory/long_term.py` |
| 多级缓存 | 意图缓存 / 生成缓存 / LLM 缓存 | `memory/*.py` |
| 上下文截断 | 按 token 限制自动截断 | `agent/nodes.py` |
| 反思优化 | 自动评估 + 最多 2 次循环优化 | `agent/nodes.py` + `agent/edges.py` |
| API Key 认证 | 请求头验证 | `api/routes.py` |
| 请求限流 | 滑动窗口 60次/60秒 | `api/routes.py` |
| 全局异常处理 | 统一错误响应 | `api/routes.py` |
| 多模型支持 | 动态切换 Qwen / MiniMax 等 | `api/routes.py` |
| 生产级日志 | 请求 ID 追踪 + 耗时统计 | `api/routes.py` + `config/logger_config.py` |
| 定时任务调度 | APScheduler 自动清理过期数据 | `schedulers/*.py` |
