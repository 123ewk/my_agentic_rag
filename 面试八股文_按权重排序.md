# 面试八股文 - Agentic RAG 项目

> 本文档按面试权重排序，权重越大越可能被问到或越重要。
> 准备建议：权重 9-10 必须熟练掌握，权重 7-8 理解原理，权重 5-6 知道概念。

---

## 🔴 核心问题（权重 9-10）

### 1. RAG 原理 [权重: 10]

**问题**：你了解 RAG 吗？讲讲 RAG 的原理？为什么需要 RAG？

**参考答案**：
RAG = Retrieval-Augmented Generation（检索增强生成）

**原理**：
```
用户问题 → 检索（向量数据库） → 获取相关文档
                              ↓
        LLM ← 检索结果 + 用户问题
                              ↓
                          生成回答
```

**为什么需要 RAG**：
1. **知识时效性**：LLM 知识有截止日期，RAG 可以接入最新文档
2. **解决幻觉**：LLM 可能胡说八道，RAG 让答案基于真实文档
3. **成本低**：比微调便宜，不需要重新训练模型
4. **可解释性**：答案有文档来源，可追溯

**RAG 流程**：
1. 文档切分（chunking）
2. 向量化（embedding）
3. 存储到向量数据库
4. 用户问题向量化
5. 相似度检索
6. 构建 prompt（问题 + 检索结果）
7. LLM 生成回答

---

### 2. LangGraph 工作流设计 [权重: 10]

**问题**：你为什么用 LangGraph？工作流是怎么设计的？为什么这样设计？

**参考答案**：

**为什么用 LangGraph**：
1. LangGraph 是 LangChain 团队开发的，专门用于构建 Agent 工作流
2. 基于状态机模型，天然适合多步骤、有条件分支的流程
3. 支持循环（反思、迭代），其他框架难以实现
4. 可视化调试，节点和边清晰可见

**我的工作流设计（已升级为双模式架构）**：

**模式一：DAG 工作流（传统模式）**：
```
意图分类
    ↓
┌───┴───┐
↓       ↓
factual   multi_hop/其他
  ↓           ↓
直接生成   查询改写 → 并行检索 → BGE重排 → 工具调用(可选) → 流式生成 → 评估 → 反思(可选)
                                                              ↓
                                                          存入记忆
```

**模式二：ReAct Agent（自动切换模式）**：
```
用户问题
    ↓
Observe(观察当前状态) → Think(LLM自主决策下一步) → Act(执行选定行动)
    ↑                                                        ↓
    └──────────────── Observe(观察执行结果) ←─────────────────┘
    
    可选行动: retrieve / web_search / query_rewrite / tool_call / generate / finish
```

**为什么这样设计**：
1. **意图分类前置**：不同类型问题走不同路径，factual 跳过不必要的步骤
2. **查询改写**：提升召回率
3. **并行检索**：多查询同时执行，减少时间
4. **BGE 重排**：语义级排序，提升答案质量
5. **评估+反思**：自动检测幻觉和质量，不达标就重生成
6. **记忆系统**：多轮对话感知上下文
7. **ReAct 模式**：LLM 自主决策每一步行动，而非硬编码路由，真正实现 Agent 化
8. **自动模式切换**：默认 DAG 模式，复杂问题（score>=0.6）自动切换到 ReAct 模式

---

### 3. 召回率优化 [权重: 9]

**问题**：RAG 常见的召回率优化手段有哪些？你的项目用了哪些？

**参考答案**：

**常见优化手段**：
1. **混合检索**：向量 + 关键词（BM25），兼顾语义和精确
2. **查询改写**：HyDE、查询扩展、查询分解
3. **重排**：BGE/CrossEncoder 对初筛结果重排
4. **分块策略**：句子级 vs 段落级，不同场景不同策略
5. **Embedding 模型**：不同模型效果差异大
6. **索引优化**：HNSW vs IVF，分层索引加速

**我的项目用的**：
1. ✅ 混合检索（向量 0.6 + BM25 0.4）
2. ✅ 查询改写（Expansion + HyDE + Decomposition）
3. ✅ BGE 重排
4. ✅ 语义分块（基于 Spacy 句子边界）

**效果**：
- 混合检索召回率提升 30%~40%
- 多策略改写额外提升 30%~40%

---

### 4. LangChain vs LangGraph [权重: 9]

**问题**：你用 LangChain 还是 LangGraph？为什么？有什么区别？

**参考答案**：

| 对比 | LangChain | LangGraph |
|------|-----------|-----------|
| **定位** | LLM 应用开发框架 | Agent 工作流框架 |
| **核心概念** | Chain（链） | Graph（图）+ 状态机 |
| **适用场景** | 简单 LLM 应用（QA、摘要） | 复杂 Agent、多步骤工作流 |
| **循环支持** | 有限 | 原生支持（反思、迭代） |
| **状态管理** | 无内置状态 | 内置状态机 |

**为什么用 LangGraph**：
- 我的项目需要意图分类路由、反思机制、多轮对话记忆
- 这些都需要在多个步骤之间传递状态和循环
- LangChain 的 Chain 不适合这种复杂工作流

---

## 🟠 重要问题（权重 7-8）

### 5. 混合检索 [权重: 8]

**问题**：什么是混合检索？为什么向量检索不够，还需要 BM25？

**参考答案**：

**向量检索的局限**：
- 对语义模糊、词汇不匹配的问题效果好
- 但对专有名词、技术术语、精确关键词查询效果差

**BM25 的优势**：
- 基于词频和逆文档频率
- 对精确关键词匹配效果好
- 不理解语义，但匹配精准

**混合策略**：
```python
vector_score = vector_similarity(query_emb, doc_emb)  # 语义
bm25_score = bm25(query, doc)                          # 关键词

# 加权融合
final_score = 0.6 * norm(vector) + 0.4 * norm(bm25)
```

**为什么这样设计**：
- 两者互补：语义理解 + 精确匹配
- 权重可调：根据业务场景（技术文档 vs 日常问答）调整

---

### 6. HyDE 原理与优劣 [权重: 8]

**问题**：HyDE 是什么？为什么用？有什么优缺点？

**参考答案**：

**HyDE = Hypothetical Document Embedding**

**原理**：
```
用户问题 → LLM 生成假设性答案 → 向量化假设答案 → 检索
```

**为什么用**：
1. 假设性答案可能包含更多相关词汇
2. 生成答案的过程本身就是一次"推理"
3. 比直接检索更容易找到语义相关文档

**优缺点**：
| 优点 | 缺点 |
|------|------|
| 提升语义召回 | 多一次 LLM 调用，延迟增加 |
| 解决词汇不匹配 | 假设答案错误会导致检索偏差 |
| 对短查询特别有效 |  |

---

### 7. 幻觉检测与解决 [权重: 8]

**问题**：RAG 的幻觉问题怎么解决？你的项目怎么做的？

**参考答案**：

**幻觉产生原因**：
1. LLM 生成内容与检索结果不符
2. 检索结果不完整或不相关
3. prompt 设计不当

**解决方案**：
1. **Faithfulness 评估**：对比答案和上下文，检测不一致
2. **引用溯源**：答案必须标注来源
3. **反思机制**：不达标就重生成
4. **CRAG**：Confidence Routing，置信度低就触发搜索

**我的项目实现**：
```python
# 评估答案忠实度
faithfulness = 计算(答案中从上下文来的事实数) / 总事实数

if faithfulness < 0.7:
    needs_reflection = True  # 触发反思
```

---

### 8. 意图识别在 Agent 中的作用 [权重: 7]

**问题**：意图识别在 Agent 工作流中有什么作用？

**参考答案**：

**作用**：
1. **智能路由**：不同意图走不同路径
   - factual → 直接生成（跳过改写、重排）
   - multi_hop → 完整流程
   - tool_call → 工具调用

2. **性能优化**：减少不必要的 LLM 调用
   - factual 类型跳过查询改写（节省 2-3 秒）
   - factual 类型跳过重排 API（节省 1-2 秒）

3. **资源分配**：复杂问题分配更多计算资源

**我的实现**：
- 4 类意图：factual、reasoning、summary、multi_hop
- 基于 prompt 让 LLM 分类
- 结果缓存避免重复调用

**ReAct 模式下的变化**：
- DAG 模式中，意图识别是必需的前置节点，决定后续路径
- ReAct 模式中，意图识别不再是独立节点——Agent 在 Think 阶段自然理解意图并选择行动
- 例如：Agent 观察到"这是一个简单事实问题"，直接选择 generate 行动，无需经过意图分类节点

---

### 9. 反思机制 [权重: 7]

**问题**：反思机制是怎么工作的？为什么需要反思？

**参考答案**：

**为什么需要反思**：
一次性生成的回答不一定最优。反思让模型：
1. 评估当前答案质量
2. 识别问题（幻觉、不完整、跑题）
3. 修正并重新生成

**DAG 模式下的反思实现**：
```python
# 评估阶段
evaluation = evaluate(response, context, question)

# 如果质量不达标
if evaluation.faithfulness < 0.7 or evaluation.relevancy < 0.6:
    needs_reflection = True

# 反思阶段
if needs_reflection:
    reflection_prompt = f"""请反思以下回答的质量：
    原始问题：{question}
    原始回答：{original_answer}
    评估结果：{evaluation}
    请生成改进后的回答"""
    refined = llm.invoke(reflection_prompt)
```

**DAG 反思的局限**：
- 伪循环：反思不改变检索策略，只是让 LLM 重写答案
- 没有策略变化：不会换工具、不会调整查询
- 反思后答案经常变短（说明反思在"压缩"而非"改进"）

**ReAct 模式下的反思（自然实现）**：
- 不需要独立的 reflection 节点
- Agent 在 Observe 阶段发现答案不好 → Think 阶段决定换策略 → Act 阶段重新检索或搜索
- 策略真正变化：从"重写答案"变为"换检索策略/换工具/补充信息"

---

### 10. 向量数据库原理 [权重: 7]

**问题**：向量数据库是如何实现高效检索的？HNSW 和 IVF 有什么区别？

**参考答案**：

**核心原理**：近似最近邻（ANN）搜索

**常用算法**：
1. **HNSW**（Hierarchical Navigable Small World）
   - 分层图结构
   - 从顶层快速定位，再逐层细化
   - 优点：查询快、精度高
   - 缺点：内存占用大

2. **IVF**（Inverted File Index）
   - 先聚类，查询时只搜索相邻聚类
   - 优点：内存占用小
   - 缺点：精度略低

**我用的**：
- Milvus：支持 HNSW、IVF 等多种索引
- pgvector：PostgreSQL 内置，HNSW 索引

---

## 🟡 进阶问题（权重 5-6）

### 11. 分块策略 [权重: 6]

**问题**：文档分块有哪些策略？为什么重要？

**参考答案**：

**常见策略**：
1. **固定大小分块**：简单，但可能切断句子
2. **句子级分块**：保持句子完整性
3. **段落级分块**：保留语义完整
4. **语义分块**：基于句子边界 + 长度限制

**为什么重要**：
- 块太大：引入噪声，上下文溢出
- 块太小：丢失上下文，召回不完整
- 重叠分块：解决上下文断裂问题

**我的实现**：
```python
# 基于 Spacy 的句子边界检测
doc = spacy_nlp(text)
sentences = [sent.text for sent in doc.sents]

# 按句子合并 + 重叠
chunks = merge_with_overlap(sentences, chunk_size=500, overlap=50)
```

---

### 12. PostgreSQL 咨询锁 [权重: 6]

**问题**：什么是咨询锁？和 Redis 锁比有什么优劣？

**参考答案**：

**咨询锁原理**：
- 不锁定表或行，只占用一个整数 ID
- 需要显式获取和释放
- 不会自动释放（除非连接断开）

**PostgreSQL 咨询锁类型**：
- `pg_advisory_lock(id)`：阻塞获取
- `pg_try_advisory_lock(id)`：非阻塞获取
- `pg_advisory_unlock(id)`：释放

**vs Redis 锁**：
| | PostgreSQL | Redis |
|---|------------|-------|
| 优点 | 不需要额外依赖 | 性能更高 |
| 缺点 | 稍慢 | 需要维护额外服务 |
| 适用 | 已有 PG 的项目 | 高性能场景 |

**我的使用场景**：
- 文档去重（全局原子操作）
- 定时任务互斥

---

### 13. SSE vs WebSocket [权重: 6]

**问题**：你用 SSE 做流式输出，和 WebSocket 有什么区别？为什么选 SSE？

**参考答案**：

| | SSE | WebSocket |
|---|-----|-----------|
| **方向** | 单向（服务端→客户端） | 双向 |
| **复杂度** | 简单 | 复杂 |
| **兼容性** | 需要 polyfill | 主流浏览器支持 |
| **自动重连** | 内置 | 需要手动实现 |
| **适用场景** | 实时推送、流式响应 | 聊天、游戏 |

**为什么选 SSE**：
1. 我的场景只需要服务端推送，不需要客户端发送数据
2. SSE 实现简单，HTTP 协议，无需 WebSocket 握手
3. 自动重连，稳定性好
4. FastAPI 原生支持 SSE

---

### 14. 重排模型原理 [权重: 5]

**问题**：BGE 重排模型是怎么工作的？为什么需要重排？

**参考答案**：

**为什么需要重排**：
- 向量检索只能保证"大致相关"，顺序不一定最优
- Top-K 检索结果中，可能有噪声文档
- 重排做精细化排序

**CrossEncoder vs BiEncoder**：
| | BiEncoder | CrossEncoder |
|---|-----------|--------------|
| 方式 | 独立编码 query 和 doc | 拼接后联合编码 |
| 速度 | 快（一次编码） | 慢（需要两两计算） |
| 精度 | 中 | 高 |

**BGE Reranker 原理**：
```python
# 输入：query + document pairs
pairs = [[query, doc1], [query, doc2], ...]

# 输出：相关性分数
scores = reranker.compute_score(pairs)

# 按分数排序
ranked = sorted(zip(docs, scores), reverse=True)
```

**我的流程**：Milvus 检索 Top-20 → BGE 重排 → 返回 Top-3

---

### 15. 记忆系统设计 [权重: 5]

**问题**：短期记忆和长期记忆的区别？设计时考虑了什么？

**参考答案**：

**区别**：
| | 短期记忆 | 长期记忆 |
|---|---------|---------|
| 存储 | 当前会话 | 跨会话 |
| 时间 | 会话结束清除 | 持久化 |
| 容量 | 受 token 限制 | 可无限积累 |
| 用途 | 多轮对话上下文 | 用户偏好、知识积累 |

**设计考虑**：
1. **Token 级别截断**：而非简单消息数量，更精确控制上下文
2. **TTL 过期**：自动清理过期会话
3. **用户隔离**：每个用户只能访问自己的记忆
4. **异步写入**：不影响主流程性能

---

### 16. 系统扩展性 [权重: 5]

**问题**：如果文档量级从 1000 增长到 1 亿，你会怎么优化？

**参考答案**：

**分层索引**：
```
文档 → 分块 → 向量化
           ↓
     Milvus（Top-K 粗筛）
           ↓
     BGE 重排（精排）
           ↓
         返回
```

**具体优化**：
1. **向量数据库**：分片集群、分区索引
2. **冷热分离**：热数据内存，热数据磁盘
3. **增量索引**：新文档增量写入，不重建
4. **缓存**：Query Cache、Result Cache
5. **异步**：检索和生成并行（先返回，后优化）

---

## 🟢 基础/追问（权重 3-4）

### 17. 技术选型 [权重: 4]

**问题**：为什么选 LangChain？不用可以吗？

**参考答案**：
- 可以：直接调用 LLM API + 自己实现工作流
- LangChain 优势：统一接口、丰富生态（工具、retriever）、提示词管理
- 缺点：抽象层带来的复杂度、学习成本

---

### 18. 项目难点 [权重: 4]

**问题**：项目中遇到的最大难点是什么？

**参考答案**：
1. **DAG→ReAct重构**：将硬编码路由的工作流改造为 LLM 自主决策的 Agent 循环，需要重新设计状态管理和安全策略
2. **伪流式→真流式**：从 `llm.invoke()` + 分段 yield 改为 `llm.astream()` 逐 token 输出，需要重新设计整个流式架构
3. **fast_stream_invoke 设计**：绕过 graph.astream() 节点级流式，手动执行前置节点+llm.astream()真流式+后台异步后处理，架构复杂度大幅提升
4. **混合检索权重调优**：向量和 BM25 的权重需要实验
5. **MiniMax 思考标签**：需要正则过滤 `<think/>` 标签
6. **异步上下文管理**：Python 异步和同步代码混用
7. **模型名大小写问题**：阿里云百炼 API 模型名大小写敏感，`Qwen3.5-plus` vs `qwen3.5-plus` 导致 404

---

### 19. 个人贡献 [权重: 3]

**问题**：这个项目是你独立完成的吗？你负责什么？

**参考答案**：
- 独立完成整个项目
- 从架构设计到代码实现到部署运维

---

### 20. 未来优化方向 [权重: 3]

**问题**：如果继续优化这个项目，你会做什么？

**参考答案**：
1. 本地模型替换 API（LLM 和 Embedding）
2. 更精细的评估指标（RAGAS）
3. 多模态支持（图片、表格）
4. 分布式部署
5. 更强的缓存机制
6. Function Calling 原生支持（替代 JSON 解析驱动的工具调用）
7. Multi-Agent 协作（检索 Agent + 推理 Agent + 评估 Agent）
8. Agent 策略学习（基于历史对话优化决策策略）
9. ~~更快的流式输出~~（已通过 fast_stream_invoke 实现：首token ~3-5s）
10. ~~后台异步后处理~~（已实现：evaluation/CRAG/reflection 移到后台）

---

## 🔥 补充核心面试问题（基于项目介绍）

### 21. Agentic 工作流架构 [权重: 9]

**问题 1**：你的 Agentic 工作流是怎么设计的？和传统 RAG 有什么区别？

**参考答案**：

**传统 RAG**：固定流程（检索 → 生成），一刀切，无法智能判断用户意图。

**我的 Agentic 工作流**：
```
意图识别 → 路由判断
           ├─ factual（事实）→ 直接生成
           ├─ multi_hop（多跳）→ 查询改写 → 检索 → 重排 → 生成
           └─ 需要外部信息 → 工具调用 → 生成
```

**优势**：
- factual 类问题跳过不必要的步骤，响应时间缩短 40%
- 答案质量不达标时自动触发反思重生成
- 支持循环和自我修正

**问题 2**：为什么用状态机？有什么好处？

**参考答案**：
1. LangGraph 的条件边机制天然适合动态路由
2. 状态机便于可视化和调试
3. 每个节点职责单一，易于扩展和维护
4. 支持反思和循环，可实现自我修正

---

### 22. 混合检索策略 [权重: 8]

**问题 1**：向量检索和 BM25 检索的权重是怎么确定的？为什么是 0.6 和 0.4？

**参考答案**：

**原理**：
```python
# 向量相似度分数（语义理解）
vector_scores = similarity_search(query)

# BM25 关键词匹配分数（精确匹配）
bm25_scores = bm25_search(query)

# 加权融合（0.6向量 + 0.4关键词）
combined = 0.6 * norm(vector) + 0.4 * norm(bm25)
```

**为什么 0.6 + 0.4**：
- 向量检索擅长语义理解，BM25 擅长关键词精确匹配
- 通过实验调整权重，0.6:0.4 在大多数场景下效果最佳
- 权重可通过配置调整，适配不同业务场景（技术文档偏向关键词，日常问答偏向语义）

**问题 2**：混合检索的效果如何？

**参考答案**：
- 召回率提升 30%~40%
- 对技术术语、专有名词检索更准确
- 适应多种表达方式的用户查询

---

### 23. 查询改写与 HyDE [权重: 8]

**问题 1**：你实现了哪几种查询改写策略？分别适用于什么场景？

**参考答案**：

| 策略 | 原理 | 适用场景 |
|------|------|----------|
| **Query Expansion** | LLM 生成 3-5 个同义表述 | 词汇不匹配 |
| **HyDE** | 生成假设性答案再嵌入检索 | 语义模糊 |
| **Query Decomposition** | 分解为多个子问题 | 复杂多跳 |

**综合策略**：
```python
# 综合策略：多种改写并行，集合去重
queries = set()
queries.update(self.expansion.expand(query))      # 扩展
queries.update(self.decomposer.decompose(query)) # 分解
return list(queries)
```

**问题 2**：多策略改写为什么能提升召回率？

**参考答案**：
1. HyDE 利用 LLM 生成假设性答案，比直接检索更容易找到相关文档
2. 查询分解解决多跳推理问题
3. 多策略并行执行，综合结果去重，召回率更高
4. 可根据问题类型选择性使用，灵活可控

**效果**：多策略改写后召回率提升 30%~40%

---

### 24. 答案质量评估与反思 [权重: 8]

**问题 1**：你的系统如何检测答案质量？

**参考答案**：

**评估指标**：
```python
faithfulness:    # 忠实度（幻觉检测）
answer_relevancy: # 答案相关性
context_precision: # 上下文精确度
```

**反思触发条件**：
```python
# 如果评估分数低于阈值，触发反思重生成
if faithfulness < 0.7 or answer_relevancy < 0.6:
    needs_reflection = True
```

**问题 2**：反思机制是怎么工作的？

**参考答案**：
1. faithfulness 通过对比答案和上下文检测幻觉
2. 多维度评估覆盖完整性和相关性
3. 反思机制让模型主动修正答案，而不是简单地重写
4. 可配置阈值，根据业务需求调整严格程度

**效果**：
- 答案可信度显著提升
- 自动化质量控制减少人工审核

---

### 25. 短期记忆系统 [权重: 7]

**问题 1**：你的短期记忆系统是怎么设计的？

**参考答案**：

**基于 PostgreSQL 的生产级短期记忆系统**：
```python
class ShortTermMemory:
    # 异步连接池（支持高并发）
    self.engine = create_async_engine(
        url, pool_size=10, max_overflow=20
    )

    # 基于 Token 数量的智能截断
    async def get_context(self, session_id, max_tokens=5000):
        for msg in reversed(messages):
            if total_tokens + msg_tokens > token_limit:
                break
```

**问题 2**：为什么用 Token 级别的截断而不是消息数量？

**参考答案**：
1. PostgreSQL 持久化确保数据不丢失
2. 异步连接池支持高并发，避免阻塞
3. Token 级别的截断比消息数量更精确控制上下文长度
4. 连接池 + tenacity 重试机制保障稳定性
5. TTL 自动过期清理，无需人工维护

**效果**：
- 多轮对话连贯性显著提升
- 上下文精确控制，避免溢出
- 支持高并发多用户场景

---

### 26. 长期记忆系统 [权重: 7]

**问题 1**：长期记忆和短期记忆有什么区别？你是怎么实现的？

**参考答案**：

**基于 PostgreSQL + pgvector 的向量记忆系统**：
```python
# 使用 pgvector 余弦相似度搜索
sql = text("""
    SELECT content,
           1 - (embedding <=> :query_embedding) as similarity
    FROM long_term_memories
    WHERE user_id = :user_id
    ORDER BY embedding <=> :query_embedding
    LIMIT :k
""")
```

**问题 2**：为什么用 pgvector 而不是单独的向量数据库？

**参考答案**：
1. pgvector 内置于 PostgreSQL，无需额外部署向量数据库
2. 1024 维向量存储，兼顾精度和性能
3. 用户级数据隔离，保护隐私
4. 批量插入优化，减少数据库往返
5. HNSW 索引加速向量检索

**效果**：
- 个性化服务能力增强
- 记忆复用减少重复解释

---

### 27. 并发控制与锁机制 [权重: 7]

**问题 1**：你的系统如何处理高并发场景下的数据一致性？

**参考答案**：

**基于 PostgreSQL 的多层次锁机制**：

| 锁类型 | 原理 | 适用场景 |
|--------|------|----------|
| **行级锁** | SELECT FOR UPDATE | 特定行并发修改保护 |
| **咨询锁** | pg_advisory_lock | 跨会话全局互斥 |

**咨询锁示例**：
```python
# 咨询锁上下文管理器
async with lock_manager.advisory_lock_context(lock_id):
    await process_user_task(user_id)  # 安全执行
```

**问题 2**：为什么用 PostgreSQL 咨询锁而不是 Redis 锁？

**参考答案**：
1. 咨询锁不锁定表或行，只占用一个 ID，开销小
2. 上下文管理器自动获取/释放锁，防止死锁
3. 可设置阻塞/非阻塞模式，灵活适配业务
4. 直接利用 PostgreSQL，无需引入 Redis 等外部依赖

**效果**：
- 高并发场景稳定性保障
- 数据一致性保障

---

### 28. 流式输出架构 [权重: 7]

**问题 1**：你是如何实现流式输出的？

**参考答案**：

**实现 Server-Sent Events (SSE) 流式输出**：

**DAG 模式（伪流式）**：
```python
# DAG模式：先拿到完整文本，再分段yield模拟流式
generation_text = state.get("generation", "")
for i in range(0, len(cleaned_text), 50):
    segment = cleaned_text[i:i+50]
    yield {"type": "chunk", "content": segment}
    await asyncio.sleep(0.02)  # 模拟流式延迟
```
问题：用户感知到的是"延迟后突然快速输出"，不是真正的逐token流式。

**ReAct 模式（真流式）**：
```python
# ReAct模式：token级流式 + 中间状态可观测
async def stream_run(self, question):
    for step in range(max_steps):
        # Observe阶段：立即输出观察结果
        observation = self.observe(state)
        yield {"type": "thought", "content": f"观察: {observation}"}
        
        # Think阶段：输出决策
        action = await self.think(observation, state)
        yield {"type": "action", "content": f"行动: {action.type}"}
        
        # Act阶段：如果是generate，token级流式
        if action.type == "generate":
            async for token in self.llm.astream(prompt):
                yield {"type": "token", "content": token}  # 真正的逐token输出
```

**DAG vs ReAct 流式对比**：
| | DAG 流式 | ReAct 流式 |
|---|---------|-----------|
| 生成方式 | 伪流式（先完整后分段） | 真流式（llm.astream逐token） |
| 中间状态 | 只能流式节点完成后的状态 | 每个阶段都可流式输出 |
| 用户感知 | 延迟后突然快速输出 | 实时看到思考→行动→生成全过程 |

**问题 2**：为什么选 SSE 而不是 WebSocket？

**参考答案**：
1. SSE 比 WebSocket 更轻量，无需双向通信
2. 后端流式推送，前端实时显示，用户体验好
3. 分阶段返回状态，让用户感知系统在工作
4. 思考标签过滤兼容 MiniMax 等特殊模型

**效果**：
- 首 token 延迟降低 70%
- 处理过程透明化

---

### 28.5. 快速流式路径（fast_stream_invoke）[权重: 9]

**问题 1**：你的DAG模式是伪流式，怎么优化成真流式？

**参考答案**：

**DAG模式为什么是伪流式——三层原因**：
1. **节点层**：`generation_node` 内部用 `llm.invoke()` 同步阻塞，等全部token生成完才return
2. **图执行层**：`graph.astream()` 是节点级流式——只有节点return后才yield该节点输出
3. **API层**：stream_invoke拿到完整文本后切成50字符块模拟流式

```python
# 伪流式的本质
async for chunk in self.graph.astream(state):  # 节点级流式
    if node_name == "generation":
        # generation_node已经return完整文本了
        # 这里只是把完整文本切成50字符块
        for i in range(0, len(text), 50):
            yield {"type": "chunk", "content": text[i:i+50]}
            await asyncio.sleep(0.02)  # 伪流式！
```

**优化方案：fast_stream_invoke（绕过graph直接用llm.astream()）**：

```python
async def fast_stream_invoke(self, question, session_id, user_id):
    # 1. 前置节点手动执行（不走graph）
    intent_result = intent_and_rewrite_node(state, self.llm, self.intent_cache)
    retrieval_result = parallel_retrieval_node(state, self.vectorstore)
    rerank_result = rerank_node(state, self.reranker)
    
    # 2. 构建prompt
    context = _build_context_parts(memory, docs, ...)
    prompt = self.prompt_template.format(context=context, question=question)
    
    # 3. 真流式生成！直接调用llm.astream()
    async for chunk in self.llm.astream(prompt):  # 逐token输出
        yield {"type": "token", "content": chunk.content}
    
    # 4. 后台异步（不阻塞用户）
    asyncio.create_task(self._background_post_processing(state))
    
    yield done()
```

**效果**：
- 首token时间从 ~40s 降至 ~3-5s
- 缓存命中时 ~0.5s

**问题 2**：为什么要把evaluation/CRAG/reflection移到后台？

**参考答案**：

核心设计原则：**用户感知速度 > 理论最优答案**

| 组件 | 移到后台的原因 | 影响 |
|------|---------------|------|
| evaluation | 改为轻量级规则评估（无LLM调用），仅用于日志监控 | 不影响已输出的答案 |
| CRAG | 第一次生成已基于检索文档，质量通常足够 | 低置信度时不重新生成，仅记录日志 |
| reflection | 质量提升有限（<5%），但增加3-5s延迟 | 快速路径下禁用 |
| 记忆保存 | 纯写操作，与用户读取无关 | 无影响 |

如果用户需要高质量答案，可以设置 `use_fast_path=False` 走标准DAG路径（包含完整evaluation→CRAG→reflection流程）。

**问题 3**：fast_stream_invoke 和 ReAct 的真流式有什么区别？

**参考答案**：

| 维度 | fast_stream_invoke | ReAct stream_run |
|------|-------------------|------------------|
| 架构 | DAG模式的手动执行路径 | ReAct Agent自主决策循环 |
| 流式方式 | llm.astream() 逐token | llm.astream() 逐token |
| 决策方式 | 硬编码路由（intent→retrieval→generation） | LLM自主决策（Observe→Think→Act） |
| 适用场景 | 延迟敏感的生产环境 | 复杂推理、探索性任务 |
| 中间状态 | status事件（阶段更新） | thought/action/observation事件 |
| 后处理 | 后台异步（不阻塞） | 循环内同步执行 |

**两者都实现了真正的token级流式，区别在于架构和决策方式。**

---

### 29. AI 响应速度优化 [权重: 8]

**问题 1**：你的系统响应时间优化了哪些？效果如何？

**参考答案**：

**问题**：DeepSeek 18秒、MiniMax 40秒，响应慢

**第三轮优化（响应速度优先架构）**：

在之前的优化基础上，新增 **fast_stream_invoke 快速流式路径**：

| # | 优化项 | 改动 | 收益 |
|---|--------|------|------|
| 1 | **真流式生成** | `fast_stream_invoke` 用 `llm.astream()` 替代 `llm.invoke()` | -10s 首token |
| 2 | **后台评估** | evaluation 移到 `_background_post_processing` | -0.5s |
| 3 | **移除CRAG阻塞** | 快速路径下 CRAG 不触发重新生成 | -10s（最坏） |
| 4 | **移除reflection阻塞** | 快速路径下禁用 reflection | -3s |
| 5 | 生成缓存快速路径（已有） | 缓存命中绕过所有 LLM 调用 | <0.5s |
| 6 | Factual 类型短路（已有） | 跳过查询改写和重排 | 2-3s |
| 7 | 并行检索（已有） | 多查询并行执行 | -2s |

**优化前 vs 优化后时间线**：
```
优化前 stream_invoke:
0s──────────────────────────────────────────────────────────40s
[记忆][意图+改写][检索][重排][generation阻塞..............][eval][CRAG?][refl?]
                                                            ↑ 用户等到这里

优化后 fast_stream_invoke:
0s──────────────────────────────────5s─────────────────────15s
[记忆][意图+改写][检索][重排][首token!→token→token→token→...]
                              ↑ 用户在这里就看到答案了!
                              
后台: [eval][记忆保存]  ← 不阻塞用户
```

**效果**：
- MiniMax 从 40秒降至 3-5秒首token（**降低 87%**）
- 完整答案 10-15s（但用户已在阅读中）
- 重复问题 **<0.5秒**

**问题 2**：缓存是怎么实现的？

**参考答案**：
- 缓存命中直接返回结果，绕过所有 LLM 调用
- 对于重复问题，响应时间从几十秒降至 0.5 秒以内
- 缓存策略可根据业务需求调整（时间过期、大小限制等）

---

### 29.5. 并行执行优化（ParallelExecutor）[权重: 9]

**问题 1**：你的系统中哪些步骤可以并行执行？哪些必须串行？为什么？

**参考答案**：

**可并行的步骤**（无数据依赖）：

| 并行组 | 任务 | 为什么可以并行 |
|--------|------|---------------|
| **Memory + Retrieval** | 短期记忆加载 / 长期记忆搜索 / 向量检索 | 三者输入都是原始问题，互不依赖 |
| **Multi-Query 检索** | 每个改写查询独立检索 | 查询之间无依赖，结果最后合并去重 |
| **Web Search** | DuckDuckGo 搜索 | 与本地检索无关 |

**必须串行的步骤**（有数据依赖）：

| 串行依赖 | 原因 |
|---------|------|
| intent_and_rewrite → retrieval | 意图决定是否需要检索、用什么策略 |
| retrieval → rerank | rerank 依赖检索结果 |
| rerank + memory → generation | 生成依赖上下文和检索文档 |
| generation → evaluation | 评估依赖生成结果 |

**问题 2**：你如何实现最大化并行？用了什么技术？

**参考答案**：

**核心组件：ParallelExecutor**

```python
class ParallelExecutor:
    """并行执行器 - 协调所有可并行的任务"""
    
    async def execute_full_pipeline(self, question, intent, ...):
        # 构建并行任务列表
        tasks = []
        
        # 1. Memory 加载（与 Retrieval 无依赖，可并行）
        if session_id or user_id:
            tasks.append(self._execute_memory_tasks(...))
        
        # 2. Retrieval 任务（与 Memory 无依赖，可并行）
        if intent in ("factual", "multi_hop", "reasoning"):
            tasks.append(self._execute_retrieval_tasks(...))
        
        # 3. Web Search（可选，与上述无依赖）
        if enable_web_search:
            tasks.append(self._execute_web_search(...))
        
        # 关键：所有无依赖任务并行执行
        await asyncio.gather(*tasks, return_exceptions=True)
        
        # 4. Rerank（依赖 retrieval 完成，必须串行）
        if result.retrieved_docs and self.reranker:
            reranked = await asyncio.to_thread(self.reranker.rerank, ...)
```

**关键技术**：
1. `asyncio.gather()` - 并行执行多个协程
2. `asyncio.to_thread()` - 将同步函数（rerank、similarity_search）包装为异步
3. `ParallelExecutionResult` - 统一收集并行任务结果

**问题 3**：并行执行有没有坑？怎么处理异常？

**参考答案**：

**常见坑**：
1. **共享状态竞争**：多个并行任务同时写 `result.retrieved_docs`
   - 解决：每个任务写不同的字段，或用 `extend` 追加（Python GIL 保证原子性）
2. **异常传播**：`asyncio.gather` 默认一个失败全部失败
   - 解决：`return_exceptions=True`，异常收集到 `result.errors` 列表
3. **同步函数阻塞事件循环**：rerank、similarity_search 是同步的
   - 解决：用 `asyncio.to_thread()` 包装，在线程池中执行

**问题 4**：ultra_fast_stream_invoke 和 fast_stream_invoke 有什么区别？

**参考答案**：

| 维度 | fast_stream_invoke | ultra_fast_stream_invoke |
|------|-------------------|--------------------------|
| Memory 加载 | 串行（先 Memory 后 Retrieval） | **并行**（Memory + Retrieval 同时启动） |
| 检索方式 | 手动调用 `parallel_retrieval_node` | 使用 `ParallelExecutor` 统一调度 |
| Web Search | 不参与并行 | **与 Memory/Retrieval 三路并行** |
| Rerank | 手动调用 `rerank_node` | ParallelExecutor 内部自动执行 |
| 首 Token 延迟 | ~3-5s | **~2-3s**（节省 Memory 等待时间） |

**核心区别**：`fast_stream_invoke` 仍然串行执行 Memory→Retrieval，而 `ultra_fast_stream_invoke` 通过 `ParallelExecutor` 实现了 Memory + Retrieval + WebSearch 的三路并行，进一步降低首 token 延迟。

---

### 30. FastAPI 异步编程 [权重: 7]

**问题 1**：为什么用 FastAPI？有什么优势？

**参考答案**：

**FastAPI 优势**：
1. **异步支持**：原生支持 async/await，高并发场景性能优秀
2. **自动文档**：自动生成 OpenAPI 文档，方便调试和集成
3. **流式响应**：原生支持 SSE 流式输出
4. **类型提示**：基于 Python 类型提示，代码更健壮
5. **性能**：基于 Starlette 和 Pydantic，性能接近 Node.js 和 Go

**问题 2**：你的项目如何使用 SQLAlchemy 异步？

**参考答案**：
```python
# 异步 ORM
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession

self.engine = create_async_engine(
    url, pool_size=10, max_overflow=20
)

# 异步会话
async with AsyncSession(self.engine) as session:
    result = await session.execute(query)
```

---

### 31. 多模型集成 [权重: 6]

**问题 1**：你的系统支持哪些模型？如何实现多模型切换？

**参考答案**：

**支持的模型**：
- Qwen（通义千问）
- GLM（智谱清言）
- MiniMax

**实现方式**：
- 通过 OpenAI 兼容 API 统一接口
- 多模型切换配置，根据需求选择最优模型
- 不同模型有不同的特点和适用场景

**问题 2**：不同模型有什么优缺点？

**参考答案**：
| 模型 | 优势 | 劣势 |
|------|------|------|
| **Qwen** | 中文理解强，成本低 | 复杂推理稍弱 |
| **GLM** | 综合能力强 | 响应速度一般 |
| **MiniMax** | 生成质量好 | 响应慢（40秒） |

---

### 32. 项目亮点总结 [权重: 9]

**问题**：请用 1-2 分钟介绍一下你的项目亮点。

**参考答案**（简洁版）：

> 基于 LangGraph 构建四路径 Agentic RAG 智能知识库系统（超快速并行 / 快速流式 / DAG / ReAct）。超快速并行路径（ultra_fast_stream_invoke）通过 ParallelExecutor 实现 Memory + Retrieval + WebSearch 三路并行，首 token 时间降至 ~2-3s。快速流式路径（fast_stream_invoke）绕过 graph 节点级流式，直接用 `llm.astream()` 实现逐 token 输出，首 token 时间 ~3-5s。ReAct 模式下 LLM 自主决策每一步行动，替代硬编码路由规则。通过 SafetyPolicy + HallucinationGuard 防止 Agent 失控。

**详细版**：

> 独立完成 Agentic RAG 系统的设计与实现，核心架构从 DAG 工作流重构为四路径系统。DAG 模式基于 LangGraph 状态机实现意图识别→查询改写→混合检索→BGE 重排→答案生成→质量评估→反思修正的完整链路；ReAct 模式基于 Observe→Think→Act 循环，LLM 自主决策每一步行动，替代硬编码路由规则。**快速流式路径（fast_stream_invoke）**：将检索、搜索、改写等 DAG 节点封装为手动执行，generation 直接用 `llm.astream()` 实现真正的 token 级流式，evaluation/CRAG/reflection 全部移到后台异步执行，首 token 时间从 ~40s 降至 ~3-5s（降低 87%）。**超快速并行路径（ultra_fast_stream_invoke）**：通过 ParallelExecutor 实现 Memory + Retrieval + WebSearch 三路并行，使用 `asyncio.gather()` + `asyncio.to_thread()` 统一调度，首 token 时间进一步降至 ~2-3s（降低 93%）。将检索、搜索、改写等 DAG 节点封装为 Tool，统一注册到 ToolRegistry，Agent 像调用 calculator 一样调用检索。采用三层防护机制（SafetyPolicy 计数器 + HallucinationGuard 幻觉检测 + Error Signal 回流）防止 Agent 失控。四路径通过配置切换，上层 API 无需修改。

---

## 📋 快速问答卡

### RAG 核心三问
1. **RAG 是什么**：检索增强生成，让 LLM 基于检索结果回答
2. **为什么用 RAG**：解决幻觉、知识时效性、成本低
3. **RAG 流程**：切分 → 向量化 → 存储 → 检索 → 构建 prompt → 生成

### LangGraph 三问
1. **为什么用**：复杂工作流、状态机、反思机制
2. **核心概念**：状态（state）、节点（node）、边（edge）
3. **和 Chain 的区别**：支持循环、状态管理

### 延迟优化三问
1. **延迟高怎么办**：缓存、并行、减少不必要的步骤
2. **DAG伪流式怎么优化**：用fast_stream_invoke绕过graph，直接llm.astream()实现真流式
3. **首token时间太长**：evaluation/CRAG/reflection移到后台异步，先给用户答案再评估；Memory+Retrieval+WebSearch三路并行（ParallelExecutor）

### 项目亮点三问
1. **最大亮点**：四路径架构（超快速并行 + 快速流式 + DAG + ReAct），首 token 时间从 ~40s 降至 ~2-3s（降低 93%）
2. **技术难点**：DAG→ReAct重构、伪流式→真流式改造（fast_stream_invoke）、并行执行器设计（ParallelExecutor）、后台异步后处理设计、混合检索权重调优、并发控制、Agent防崩设计
3. **效果数据**：召回率提升 30%~40%，首 token 延迟降低 93%，重复请求 <0.5秒，真流式逐 token 输出，Memory+Retrieval并行节省2-3s

---

## 🚀 项目延伸问题（面试官可能联想到的追问）

> 以下问题不是直接问你的项目，但面试官可能从你的项目经验出发，延伸到相关领域考察你的知识广度。

### 33. RAG 架构演进 [权重: 9]

**问题**：你知道 RAG 有哪些演进版本？Naive RAG、Advanced RAG、Modular RAG 有什么区别？

**参考答案**：

**Naive RAG（朴素 RAG）**：
```
检索 → 生成（简单流程）
```
- 缺点：检索质量差、容易幻觉、无法处理复杂问题

**Advanced RAG（高级 RAG）**：
```
查询改写 → 混合检索 → 重排 → 生成
```
- 改进：查询优化、检索优化、生成优化

**Modular RAG（模块化 RAG）**：
```
多个可插拔模块：检索器、阅读器、记忆模块、工具模块
```
- 特点：灵活组合、可迭代优化

**Agentic RAG（我的项目）**：
```
DAG模式: 意图识别 → 动态路由 → 多策略处理 → 评估反思
ReAct模式: Observe → Think(自主决策) → Act(执行行动) → 循环
```
- 特点：智能决策、自我修正、工具调用、真流式输出

---

### 34. Embedding 模型原理 [权重: 8]

**问题**：Embedding 模型是怎么工作的？如何选择合适的 Embedding 模型？

**参考答案**：

**原理**：
- 将文本映射为固定维度的向量（如 1024 维）
- 语义相似的文本，向量距离更近
- 常用模型：OpenAI ada-002、智谱 embedding、BGE、M3E

**选择标准**：
| 维度 | 说明 |
|------|------|
| **维度** | 维度越高精度越好，但计算成本越高 |
| **语言支持** | 中文场景需要选中文优化的模型 |
| **性能** | 推理速度、API 延迟、成本 |
| **效果** | 在 MTEB 等基准测试上的表现 |

**我的项目**：使用智谱 AI embedding（1024 维），中文效果好

---

### 35. 多轮对话管理 [权重: 8]

**问题**：如何实现多轮对话？对话历史太长怎么办？

**参考答案**：

**多轮对话实现方式**：
1. **拼接历史**：将历史消息拼接到 prompt
2. **摘要记忆**：定期将历史消息摘要化
3. **向量记忆**：将历史消息向量化，按需检索

**对话历史过长处理**：
```python
# 策略 1：滑动窗口（保留最近 N 条消息）
messages = messages[-10:]

# 策略 2：Token 级别截断（我的项目用的）
total_tokens = 0
for msg in reversed(messages):
    if total_tokens + msg_tokens > token_limit:
        break

# 策略 3：摘要压缩
summary = llm.summarize(long_history)
messages = [summary] + recent_messages
```

**我的选择**：Token 级别截断，更精确控制上下文长度

---

### 36. Prompt 工程技巧 [权重: 8]

**问题**：你在项目中如何设计 Prompt？有什么技巧？

**参考答案**：

**核心技巧**：
1. **角色设定**：明确 AI 角色（"你是一个专业的知识库助手"）
2. **结构化输入**：用 XML 标签分隔不同部分（<context>、<question>）
3. **思维链（CoT）**：引导模型逐步思考（"请一步步分析..."）
4. **输出格式**：指定输出格式（JSON、列表、段落）
5. **示例学习（Few-shot）**：提供几个示例帮助理解

**我的项目 Prompt 结构**：
```python
prompt = f"""
你是一个智能知识库助手。请基于以下上下文回答问题。

<context>
{retrieved_documents}
</context>

<question>
{user_question}
</question>

要求：
1. 答案必须基于上下文
2. 如果上下文中没有答案，明确说明
3. 答案要准确、简洁
"""
```

---

### 37. Agent 工具调用原理 [权重: 7]

**问题**：Agent 是如何选择和调用工具的？

**参考答案**：

**工具调用流程**：
```
用户问题 → LLM 判断是否需要工具 → 选择工具 → 执行工具 → 整合结果 → 生成回答
```

**工具选择方式**：
1. **函数调用（Function Calling）**：LLM 直接输出工具参数
2. **ReAct 模式**：Reasoning + Acting，思考 → 行动 → 观察 → 循环
3. **Prompt 引导**：在 prompt 中描述可用工具

**DAG 模式下的工具调用（JSON解析驱动）**：
```python
# DAG模式：意图分类触发工具调用，JSON解析提取参数
chain = TOOL_CALL_PROMPT | llm | PydanticOutputParser()
response = chain.invoke({"question": question, "tools_desc": tools_desc})
# 问题：JSON解析脆弱，工具选择被意图控制，不支持多步调用
```

**ReAct 模式下的工具调用（自主决策）**：
```python
# ReAct模式：Agent在Think阶段自主决定是否调用工具
# 工具统一注册到ToolRegistry，Agent像调用calculator一样调用检索
tool_registry.register("vector_search", VectorSearchTool(...))
tool_registry.register("web_search", WebSearchTool())
tool_registry.register("calculator", calculator_tool)

# Agent自主决策
action = await self.think(observation, state)
# action.type可能是: retrieve / web_search / tool_call / generate / finish
result = await self.act(action, state)
```

**关键区别**：
| | DAG 工具调用 | ReAct 工具调用 |
|---|------------|--------------|
| 触发方式 | 意图分类 == "tool_call" | Agent自主判断"我需要更多信息" |
| 工具选择 | 硬编码路由 | LLM自主选择 |
| 多步调用 | 不支持（一次就进generation） | 天然支持（循环中可多次调用不同工具） |
| 解析方式 | JSON解析（脆弱） | 结构化Action对象（稳定） |

---

### 38. 向量数据库对比 [权重: 7]

**问题**：你知道哪些向量数据库？它们有什么优缺点？

**参考答案**：

| 向量数据库 | 优点 | 缺点 | 适用场景 |
|-----------|------|------|----------|
| **Milvus** | 十亿级、分布式、高性能 | 部署复杂、资源占用大 | 大规模生产环境 |
| **pgvector** | PostgreSQL 内置、部署简单 | 性能不如专业数据库 | 中小规模、已有 PG 的项目 |
| **Chroma** | 轻量级、易上手 | 不支持大规模 | 开发测试、原型验证 |
| **FAISS** | Facebook 开源、性能强 | 只支持本地、无持久化 | 离线计算、嵌入式场景 |
| **Pinecone** | 全托管、零运维 | 成本高、数据不在本地 | 快速上线、不想运维 |

**我的选择**：
- Milvus：主向量数据库，处理大规模文档检索
- pgvector：长期记忆，与业务数据共享数据库

---

### 39. 文档处理与切分策略 [权重: 7]

**问题**：不同格式的文档（PDF、Word、Excel）如何处理？

**参考答案**：

**文档解析**：
| 格式 | 解析库 | 难点 |
|------|--------|------|
| **PDF** | PyPDF2、pdfplumber | 表格、图片、多栏排版 |
| **Word** | python-docx | 嵌套结构、格式保留 |
| **Excel** | openpyxl、pandas | 表格转文本的语义保留 |
| **Markdown** | 直接解析 | 相对简单 |
| **HTML** | BeautifulSoup | 去除标签、保留结构 |

**切分策略**：
```python
# 语义分块（基于句子边界）
doc = spacy_nlp(text)
sentences = [sent.text for sent in doc.sents]

# 按句子合并 + 重叠
chunks = merge_with_overlap(
    sentences,
    chunk_size=500,
    overlap=50  # 重叠 50 个字符，避免上下文断裂
)
```

---

### 40. 模型部署与推理优化 [权重: 7]

**问题**：如果要部署本地模型，你会怎么做？

**参考答案**：

**本地模型部署方案**：
1. **Ollama**：一键部署，支持多种模型（Llama、Qwen、Mistral）
2. **vLLM**：高性能推理引擎，支持 PagedAttention
3. **TensorRT-LLM**：NVIDIA 官方优化，性能最强

**推理优化技术**：
| 技术 | 原理 | 效果 |
|------|------|------|
| **量化** | FP16 → INT8 → INT4 | 模型体积缩小 2-4 倍 |
| **PagedAttention** | 分页管理 KV Cache | 吞吐量提升 2-4 倍 |
| **Speculative Decoding** | 小模型草稿 + 大模型验证 | 加速 2-3 倍 |
| **Continuous Batching** | 动态批处理请求 | GPU 利用率提升 |

**我的未来优化**：替换 API 调用为本地部署，降低成本、保护数据隐私

---

### 41. 评估指标体系 [权重: 7]

**问题**：如何评估 RAG 系统的效果？

**参考答案**：

**RAGAS 评估框架**：
| 指标 | 含义 | 计算方式 |
|------|------|----------|
| **Faithfulness** | 忠实度（是否幻觉） | 答案中可从上下文推导的事实比例 |
| **Answer Relevancy** | 答案相关性 | 答案与问题的相关程度 |
| **Context Precision** | 上下文精确度 | 检索到的上下文中有用信息的比例 |
| **Context Recall** | 上下文召回率 | 检索到的上下文覆盖真实答案的比例 |

**我的项目实现**：
```python
if faithfulness < 0.7 or answer_relevancy < 0.6:
    needs_reflection = True  # 触发反思重生成
```

**其他评估方式**：
- **人工评估**：质量最高但成本高
- **A/B 测试**：对比不同策略的实际效果
- **自动化测试集**：构建标准问答对，定期回归测试

---

### 42. 知识图谱与 RAG 结合 [权重: 6]

**问题**：你知道知识图谱如何与 RAG 结合吗？

**参考答案**：

**GraphRAG 原理**：
```
文档 → 抽取实体和关系 → 构建知识图谱 → 图谱检索 + 向量检索 → 生成
```

**优势**：
- 理解实体间的关系（如"A 是 B 的子公司"）
- 支持关系查询（"A 的竞争对手有哪些"）
- 结构化知识补充向量检索的不足

**我的项目未来可以加**：
- 从文档中抽取实体和关系
- 存储到 Neo4j 等图数据库
- 检索时同时查询向量和图结构

---

### 43. 多模态 RAG [权重: 6]

**问题**：如果文档中包含图片和表格，你的系统如何处理？

**参考答案**：

**多模态 RAG 方案**：
1. **图片理解**：使用多模态 LLM（如 GPT-4V、Qwen-VL）解析图片
2. **表格提取**：使用 OCR 或表格解析库提取结构化数据
3. **统一索引**：文本、图片描述、表格数据都向量化存储

**流程**：
```
PDF 文档 → 文本分块 + 图片提取 + 表格提取
              ↓
        分别向量化
              ↓
        统一检索 → 文本 + 图片描述 + 表格数据
              ↓
         多模态 LLM 生成回答
```

**我的项目未来优化方向**：支持图片、表格等多模态内容

---

### 44. API 设计与最佳实践 [权重: 6]

**问题**：你的 API 是如何设计的？有什么最佳实践？

**参考答案**：

**RESTful API 设计原则**：
```python
# 好的 API 设计
GET    /api/conversations/{id}/messages    # 获取消息列表
POST   /api/conversations                  # 创建会话
POST   /api/conversations/{id}/ask         # 提问（非流式）
POST   /api/conversations/{id}/ask-stream  # 提问（流式）
DELETE /api/conversations/{id}             # 删除会话
```

**最佳实践**：
1. **版本控制**：`/api/v1/...`
2. **错误处理**：统一错误格式 `{"code": 400, "message": "..."}`
3. **分页**：`?page=1&size=20`
4. **限流**：Token Bucket 或滑动窗口
5. **认证**：JWT Token
6. **文档**：OpenAPI/Swagger 自动生成

**我的项目**：FastAPI 自动生成 OpenAPI 文档，支持流式响应

---

### 45. 系统监控与可观测性 [权重: 6]

**问题**：生产环境如何监控系统运行状态？

**参考答案**：

**可观测性三大支柱**：
| 维度 | 工具 | 作用 |
|------|------|------|
| **日志（Logs）** | Loguru、ELK | 记录事件、排查问题 |
| **指标（Metrics）** | Prometheus + Grafana | 监控性能指标（QPS、延迟、错误率） |
| **链路追踪（Tracing）** | Jaeger、OpenTelemetry | 追踪请求全链路耗时 |

**关键监控指标**：
```python
# 应用层指标
- 请求 QPS
- 响应延迟（P50/P95/P99）
- 错误率
- 缓存命中率
- 检索召回率

# 资源层指标
- CPU 使用率
- 内存使用率
- 数据库连接池
- 向量数据库 QPS
```

**我的项目**：使用 Loguru 结构化日志，未来可加 Prometheus 监控

---

### 46. CI/CD 与部署流程 [权重: 5]

**问题**：你的项目如何部署？有 CI/CD 吗？

**参考答案**：

**CI/CD 流程**：
```
代码提交 → 单元测试 → 集成测试 → 构建镜像 → 部署到测试环境 → 部署到生产
```

**部署方案**：
1. **Docker**：容器化，环境一致
2. **Docker Compose**：本地多服务编排（API + PostgreSQL + Milvus）
3. **Kubernetes**：生产环境，自动扩缩容
4. **CI 工具**：GitHub Actions、GitLab CI

**我的项目**：目前本地开发，未来可容器化部署

---

### 47. 安全与隐私保护 [权重: 5]

**问题**：知识库系统需要注意哪些安全问题？

**参考答案**：

**安全要点**：
| 类别 | 措施 |
|------|------|
| **认证** | JWT Token 验证用户身份 |
| **授权** | 用户只能访问自己的会话和记忆 |
| **数据加密** | HTTPS 传输、数据库加密存储 |
| **输入校验** | 防 SQL 注入、XSS 攻击 |
| **API 限流** | 防止恶意刷接口 |
| **隐私保护** | 不记录、不存储敏感信息 |

**我的项目**：
- 用户级数据隔离（WHERE user_id = ?）
- PostgreSQL 咨询锁保障并发安全
- 连接池 + 异步操作防止阻塞

---

### 48. APScheduler 定时任务调度 [权重: 7]

**问题**：你的项目如何实现定时任务？为什么用 APScheduler？

**参考答案**：

**为什么选 APScheduler**：
1. **功能完善**：支持 Cron、Interval、Date 多种触发器
2. **后台运行**：BackgroundScheduler 不阻塞主线程
3. **持久化**：可配合数据库持久化任务状态
4. **轻量级**：无需 Redis 等外部依赖

**我的项目定时任务设计**：
```python
from apscheduler.schedulers.background import BackgroundScheduler
from apscheduler.triggers.cron import CronTrigger

class TaskScheduler:
    """定时任务调度器单例类"""
    _instance = None
    _scheduler = None
    
    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance
    
    def add_cleanup_expired_task(self, short_term_memory):
        """添加清理过期会话的定时任务"""
        def cleanup_task():
            loop = asyncio.new_event_loop()
            asyncio.set_event_loop(loop)
            try:
                deleted_count = loop.run_until_complete(
                    short_term_memory.cleanup_expired()
                )
            finally:
                loop.close()
        
        # 每天凌晨2点执行
        self._scheduler.add_job(
            cleanup_task,
            CronTrigger(hour=2, minute=0, timezone="Asia/Shanghai"),
            id="cleanup_expired_sessions",
            name="清理过期会话",
            replace_existing=True
        )
```

**定时任务列表**：
| 任务 | 执行时间 | 功能 |
|------|----------|------|
| 清理过期会话 | 每天凌晨 2:00 | 删除 TTL 过期的短期会话 |
| 清理旧长期记忆 | 每天凌晨 3:30 | 删除 90 天前的记忆 + 去重 |

---

### 49. 定时任务中的异步处理 [权重: 8]

**问题**：APScheduler 是同步的，你如何在定时任务中执行异步代码？

**参考答案**：

**问题背景**：
APScheduler 的 job 函数默认是同步执行的，但我的项目大量使用 async/await 异步代码。

**解决方案**：
```python
def cleanup_task():
    """定时任务入口（同步函数）"""
    try:
        # 创建新的事件循环
        loop = asyncio.new_event_loop()
        asyncio.set_event_loop(loop)
        try:
            # 在新循环中执行异步任务
            deleted_count = loop.run_until_complete(
                short_term_memory.cleanup_expired()
            )
        finally:
            # 确保关闭循环
            loop.close()
    except Exception as e:
        logger.error(f"清理任务执行失败：{e}")
```

**为什么需要这样做**：
1. APScheduler 在主线程运行，不支持原生的 async job
2. 异步函数必须在事件循环中执行
3. 每次创建新循环避免与主程序冲突

**进阶方案 - asyncio.to_thread**：
```python
# 适用于简单异步操作
async def async_cleanup():
    return await short_term_memory.cleanup_expired()

def cleanup_task():
    result = asyncio.run(async_cleanup())
```

---

### 50. 单例模式在定时任务中的应用 [权重: 7]

**问题**：为什么你的 TaskScheduler 要用单例模式？

**参考答案**：

**为什么用单例**：
```python
class TaskScheduler:
    _instance = None
    _scheduler = None
    
    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance
```

**单例的好处**：
1. **全局唯一**：确保整个进程只有一个调度器实例
2. **资源节约**：避免重复创建 BackgroundScheduler
3. **状态一致**：所有定时任务由同一个调度器管理
4. **防止重复调度**：任务 ID 唯一，避免重复添加

**应用场景**：
- 定时任务调度器只需要一个
- 数据库连接池通常也用单例
- 配置管理器用单例

---

### 51. 定时任务的优雅关闭 [权重: 7]

**问题**：如何优雅地关闭定时任务调度器？

**参考答案**：

**优雅关闭方案**：
```python
def shutdown(self):
    """停止调度器"""
    if self._scheduler.running:
        # wait=False：不等待正在执行的任务完成
        # wait=True：等待所有任务完成后才关闭
        self._scheduler.shutdown(wait=True)
        logger.info("定时任务调度器已停止")
```

**shutdown(wait=True) vs shutdown(wait=False)**：
| 参数 | 行为 | 适用场景 |
|------|------|----------|
| wait=True | 等待当前任务完成 | 任务可中断、有幂等性保证 |
| wait=False | 立即停止 | 任务耗时较长、不关心完成 |

**在我的项目中的实践**：
```python
# 启动时
scheduler.start()

# 关闭时（如进程收到 SIGTERM 信号）
import signal
def signal_handler(signum, frame):
    scheduler.shutdown(wait=True)  # 优雅等待
    sys.exit(0)

signal.signal(signal.SIGTERM, signal_handler)
```

---

### 52. 定时任务与数据库连接池 [权重: 7]

**问题**：定时任务中如何处理数据库连接？有什么坑？

**参考答案**：

**连接管理问题**：
定时任务执行时间长，可能遇到连接超时、断连等问题。

**解决方案**：
```python
def cleanup_task():
    loop = asyncio.new_event_loop()
    asyncio.set_event_loop(loop)
    try:
        # 每次任务创建新连接
        result = loop.run_until_complete(
            short_term_memory.cleanup_expired()
        )
    finally:
        loop.close()  # 确保释放连接
```

**常见坑**：
1. **连接超时**：长定时任务执行中连接断开
   - 解决：使用连接池 + 重试机制

2. **事件循环冲突**：
   - 解决：定时任务使用独立的 asyncio 事件循环

3. **SQLAlchemy 异步会话泄漏**：
   - 解决：确保每个任务正确关闭 session

**我的项目实践**：
- ShortTermMemory 和 LongTermMemory 使用异步连接池
- 定时任务每次创建独立的事件循环
- 任务执行完必须关闭循环释放资源

---

### 53. 定时任务的监控与日志 [权重: 6]

**问题**：如何监控定时任务的执行情况？

**参考答案**：

**日志记录策略**：
```python
def cleanup_task():
    """清理过期会话的任务"""
    try:
        logger.info("开始执行清理过期会话任务...")
        deleted_count = loop.run_until_complete(
            short_term_memory.cleanup_expired()
        )
        logger.info(f"清理过期会话任务完成，删除了 {deleted_count} 条记录")
    except Exception as e:
        logger.error(f"清理过期会话任务执行失败：{e}")
```

**关键日志节点**：
| 阶段 | 日志级别 | 内容 |
|------|----------|------|
| 开始 | INFO | 任务开始执行 |
| 进度 | DEBUG | 子任务进度 |
| 完成 | INFO | 执行结果（删除数量） |
| 失败 | ERROR | 异常信息和堆栈 |

**监控指标**：
- 任务执行频率是否符合预期
- 每次执行删除的数据量
- 任务执行耗时
- 任务失败次数和原因

---

### 54. 定时任务与系统集成 [权重: 6]

**问题**：你的定时任务系统如何与 FastAPI 应用集成？

**参考答案**：

**集成架构**：
```
main.py
    │
    ├── initialize_components()     # 初始化所有组件
    │       │
    │       ├── ShortTermMemory     # 短期记忆
    │       └── LongTermMemory      # 长期记忆
    │
    ├── setup_short_term_scheduler()  # 启动短期记忆定时任务
    │
    ├── setup_long_term_scheduler()   # 启动长期记忆定时任务
    │
    └── uvicorn.run()               # 启动 FastAPI 服务
```

**启动流程**：
```python
def main():
    # 1. 初始化所有组件
    components = initialize_components()
    
    # 2. 配置定时任务调度器
    short_term_scheduler = setup_short_term_scheduler(
        components["short_term_memory"]
    )
    long_term_scheduler = setup_long_term_scheduler(
        components["long_term_memory"]
    )
    
    # 3. 启动 FastAPI 服务
    uvicorn.run(app, host="127.0.0.1", port=8000)
    
    # 4. 进程结束时自动调用 scheduler.shutdown()
```

**生命周期管理**：
- 调度器在服务启动时初始化
- 与 FastAPI 应用共用进程
- 通过信号处理实现优雅关闭

---

## 🧠 AI 方向核心知识点

> 以下是不依赖具体项目但面试常考的 AI 基础知识。

### 48. Transformer 架构原理 [权重: 10]

**问题**：请解释 Transformer 架构的核心原理

**参考答案**：

**核心组件**：
1. **Self-Attention（自注意力）**：
```
Attention(Q, K, V) = softmax(QK^T / sqrt(d_k))V
```
- Q（Query）：查询向量
- K（Key）：键向量
- V（Value）：值向量
- 作用：让模型关注输入中最重要的部分

2. **Multi-Head Attention（多头注意力）**：
- 并行多组注意力，捕捉不同维度的关系

3. **Positional Encoding（位置编码）**：
- 为序列添加位置信息（正弦/余弦函数）

4. **Feed-Forward Network（前馈网络）**：
- 两层全连接，非线性变换

5. **Layer Normalization + Residual Connection**：
- 稳定训练、加速收敛

---

### 56. LLM 训练流程 [权重: 9]

**问题**：大语言模型是如何训练的？

**参考答案**：

**训练三阶段**：
1. **Pre-training（预训练）**：
   - 数据：海量无标注文本
   - 目标：Next Token Prediction（预测下一个词）
   - 结果：获得通用语言理解能力

2. **SFT（Supervised Fine-Tuning，有监督微调）**：
   - 数据：高质量指令-回答对
   - 目标：让模型遵循指令
   - 结果：获得对话和任务能力

3. **RLHF（Reinforcement Learning from Human Feedback）**：
   - 数据：人类偏好排序
   - 目标：对齐人类价值观
   - 结果：回答更安全、有用

---

### 50. 常见 LLM 对比 [权重: 8]

**问题**：你知道哪些主流大模型？它们有什么特点？

**参考答案**：

| 模型 | 厂商 | 参数规模 | 特点 |
|------|------|----------|------|
| **GPT-4** | OpenAI | 未知（万亿级） | 综合能力最强，多模态 |
| **Claude 3** | Anthropic | 未知 | 长上下文（200K），安全 |
| **Llama 3** | Meta | 8B/70B | 开源，生态好 |
| **Qwen 2.5** | 阿里 | 0.5B-72B | 中文优化，开源 |
| **GLM-4** | 智谱 | 未知 | 中文能力强，工具调用 |
| **MiniMax** | MiniMax | 未知 | 长文本、多模态 |

---

### 51. Prompt 设计模式 [权重: 8]

**问题**：有哪些常见的 Prompt 设计模式？

**参考答案**：

**常见模式**：
1. **Zero-shot**：直接提问，不给示例
   ```
   请翻译：Hello World
   ```

2. **Few-shot**：提供几个示例
   ```
   输入：你好 → 输出：Hello
   输入：世界 → 输出：World
   输入：今天天气不错 → 输出：
   ```

3. **Chain-of-Thought（CoT，思维链）**：
   ```
   请一步步思考：小明有 5 个苹果，给了小红 2 个，还剩几个？
   ```

4. **ReAct（Reason + Act）**：
   ```
   思考：我需要先搜索...
   行动：调用搜索工具...
   观察：得到结果...
   思考：基于结果，我的回答是...
   ```

5. **Self-Consistency（自洽性）**：
   - 多次生成，选择投票最多的答案

---

### 52. RAG vs Fine-tuning [权重: 8]

**问题**：RAG 和微调有什么区别？什么时候用哪个？

**参考答案**：

| 对比 | RAG | Fine-tuning |
|------|-----|-------------|
| **原理** | 检索外部知识 + 生成 | 用特定数据训练模型 |
| **成本** | 低（不需要训练） | 高（需要 GPU 训练） |
| **知识更新** | 实时更新文档即可 | 需要重新训练 |
| **可解释性** | 高（答案有来源） | 低（知识内化在模型中） |
| **幻觉** | 少（基于检索结果） | 可能仍有幻觉 |
| **适用场景** | 知识密集型任务 | 风格/格式适配任务 |

**选择建议**：
- 知识密集、需要溯源 → RAG
- 特定风格、格式要求 → Fine-tuning
- 最好结合使用 → RAG + Fine-tuning

---

### 60. 损失函数与优化器 [权重: 7]

**问题**：大模型训练用什么损失函数和优化器？

**参考答案**：

**损失函数**：
- **Cross-Entropy Loss（交叉熵）**：用于 Next Token Prediction
```python
loss = -log(P(正确 token))
```

**优化器**：
- **AdamW**：最常用，带权重衰减的 Adam
- **学习率调度**：Cosine Annealing、Warmup

**训练技巧**：
- Gradient Accumulation（梯度累积）：模拟大 batch
- Mixed Precision（混合精度）：FP16 加速训练
- LoRA：参数高效微调

---

### 54. KV Cache 原理 [权重: 7]

**问题**：什么是 KV Cache？为什么能加速推理？

**参考答案**：

**原理**：
- LLM 生成时，每一步都需要计算所有历史 token 的注意力
- KV Cache 缓存已经计算过的 Key 和 Value
- 新 token 只需要计算一次，避免重复计算

**效果**：
- 加速比：2-4 倍
- 内存代价：需要存储 KV 缓存

**优化技术**：
- **PagedAttention**：分页管理 KV Cache（vLLM）
- **FlashAttention**：减少内存访问次数

---

### 55. 大模型量化 [权重: 6]

**问题**：什么是模型量化？有哪些量化方式？

**参考答案**：

**原理**：降低模型参数的精度，减少内存占用

**常见方式**：
| 量化方式 | 精度 | 模型体积 | 精度损失 |
|---------|------|----------|----------|
| FP16 | 16 位 | 50% | 几乎无 |
| INT8 | 8 位 | 25% | 很小 |
| INT4 | 4 位 | 12.5% | 较小 |
| GPTQ | INT4 | 12.5% | 小 |
| AWQ | INT4 | 12.5% | 最小 |

**我的项目未来可用**：本地部署时使用量化减少资源需求

---

### 56. LoRA 原理 [权重: 6]

**问题**：什么是 LoRA？为什么能高效微调？

**参考答案**：

**原理**：
- 不修改原始权重，只训练额外的低秩矩阵
- 训练时：原始权重 + 低秩矩阵输出
- 推理时：合并低秩矩阵到原始权重

**公式**：
```
W' = W + BA
```
- W：原始权重（不更新）
- B、A：低秩矩阵（训练更新）

**优势**：
- 参数少：只需训练 1%-10% 的参数
- 存储小：只存低秩矩阵
- 可切换：不同任务加载不同 LoRA

---

### 64. AI Agent 架构模式 [权重: 7]

**问题**：你知道哪些 Agent 架构模式？

**参考答案**：

**常见架构**：
1. **ReAct**：Reasoning + Acting
   - 思考 → 行动 → 观察 → 循环

2. **Plan-and-Execute**：
   - 先制定计划，再逐步执行

3. **Reflexion**：
   - 自我反思，失败后调整策略

4. **Multi-Agent**：
   - 多个 Agent 协作，各有专长

5. **CrewAI**：
   - 角色分工、任务分配、结果汇总

**我的项目用的**：双模式架构
- DAG 模式：基于 LangGraph 的状态机模式，支持意图路由和反思
- ReAct 模式：基于 Observe→Think→Act 循环的真正 Agent 系统

**DAG vs ReAct 的本质区别**：
| | DAG 工作流 | ReAct Agent |
|---|-----------|-------------|
| 决策者 | 人（硬编码路由规则） | LLM（自主决策） |
| 路径 | 固定（intent→rewrite→retrieve→...） | 动态（Agent根据观察选择行动） |
| 循环 | 伪循环（反思不改变策略） | 真循环（策略随观察变化） |
| 工具调用 | 被动（意图分类触发） | 主动（Agent自主决定） |
| 扩展性 | 加节点+改路由 | 注册新工具即可 |

**ReAct Agent 核心模块**：
```
┌─────────────────────────────────────┐
│           ReAct Agent Core          │
│  ┌──────────┐  ┌──────────┐       │
│  │ Observer  │→│  Planner  │       │
│  │ (观察器)  │  │ (规划器)  │       │
│  └──────────┘  └────┬─────┘       │
│                      ↓             │
│  ┌──────────┐  ┌──────────┐       │
│  │ Executor  │←│  Policy   │       │
│  │ (执行器)  │  │ (策略层)  │       │
│  └──────────┘  └──────────┘       │
│  ┌──────────────────────────────┐  │
│  │    Tool Registry (工具注册表) │  │
│  │  vector_search / web_search  │  │
│  │  query_rewrite / calculator  │  │
│  └──────────────────────────────┘  │
│  ┌──────────────────────────────┐  │
│  │    SafetyPolicy (安全策略)    │  │
│  │  max_steps / max_tool_calls  │  │
│  │  HallucinationGuard          │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
```

---

### 58. 函数调用（Function Calling）[权重: 7]

**问题**：什么是 Function Calling？

**参考答案**：

**原理**：
- LLM 不仅生成文本，还能输出结构化的函数调用
- 模型决定调用哪个函数、传入什么参数

**流程**：
```
用户问题 → LLM 判断需要调用函数 → 输出函数名和参数
         → 执行函数 → 返回结果给 LLM → LLM 整合生成回答
```

**支持框架**：
- OpenAI Function Calling
- Anthropic Tool Use
- Ollama 支持部分模型

**应用场景**：
- 搜索、计算、数据库查询、工具调用

---

### 66. 多模态大模型 [权重: 6]

**问题**：你知道哪些多模态大模型？

**参考答案**：

| 模型 | 支持的模态 | 特点 |
|------|-----------|------|
| **GPT-4V** | 文本 + 图片 | 理解能力强 |
| **Claude 3** | 文本 + 图片 | 长上下文 |
| **Qwen-VL** | 文本 + 图片 | 开源 |
| **MiniMax** | 文本 + 图片 + 视频 | 多模态生成 |

**应用**：
- 图片理解与描述
- 文档图片解析
- 视频内容分析

---

### 67. 开源 vs 闭源模型 [权重: 6]

**问题**：开源模型和闭源模型如何选择？

**参考答案**：

| 对比 | 开源模型 | 闭源模型 |
|------|----------|----------|
| **成本** | 免费，但需要 GPU | API 调用付费 |
| **可控性** | 完全可控，可修改 | 黑盒，无法修改 |
| **性能** | 略弱（但在追赶） | 当前最强 |
| **部署** | 需要运维 | 零运维 |
| **数据隐私** | 本地部署，数据不泄露 | 数据发送到云端 |
| **代表** | Llama、Qwen、Mistral | GPT-4、Claude 3 |

**选择建议**：
- 生产初期：闭源（快速上线）
- 生产成熟：混合（核心业务本地化）
- 长期目标：开源（降低成本、保护隐私）

---

## 🔥 ReAct Agent 重构核心问题（权重 9-10）

> 以下问题基于项目从 DAG 工作流重构为 ReAct Agent 的实际经验，面试中极可能被深入追问。

### 68. DAG 工作流 vs 真正的 Agent [权重: 10]

**问题**：你的系统原来用 DAG 工作流，现在改成了 ReAct Agent，它们本质区别是什么？为什么要改？

**参考答案**：

**本质区别——决策权在谁手里**：

| | DAG 工作流 | ReAct Agent |
|---|-----------|-------------|
| **决策者** | 人（开发者写路由规则） | LLM（自主决策下一步） |
| **路径** | 固定（intent→rewrite→retrieve→rerank→generate） | 动态（Agent根据观察选择行动） |
| **适应性** | 只能处理预定义情况 | 能处理未知情况 |
| **扩展方式** | 加节点+改路由+改边 | 注册新工具即可 |

**为什么要改**：
1. **DAG 的路由是硬编码的**：`route_after_*` 函数全是 `if/else`，LLM 只做节点内处理，不做行动决策
2. **伪循环问题**：evaluation→reflection→evaluation 循环不改变策略，只是让 LLM 重写答案
3. **工具调用被意图控制**：只有 `intent == "tool_call"` 才进入工具调用节点，Agent 不能自主发现"我需要更多信息"
4. **流式输出困难**：`llm.invoke()` 同步阻塞，只能做伪流式

**重构后的改进**：
- Agent 在 Think 阶段自主选择行动（retrieve/web_search/generate/finish）
- 循环中策略真正变化（从"重写答案"变为"换检索策略/换工具/补充信息"）
- 使用 `llm.astream()` 实现真正的 token 级流式
- 工具统一注册到 ToolRegistry，Agent 像调用 calculator 一样调用检索

---

### 69. ReAct 循环的 Observe-Think-Act [权重: 10]

**问题**：ReAct 的 Observe→Think→Act 循环具体怎么工作的？每一步输入输出是什么？

**参考答案**：

**核心循环**：
```
Observe(观察) → Think(思考) → Act(行动) → Observe(观察) → ...
```

**每一步的输入输出**：

| 步骤 | 输入 | 输出 | State 变化 |
|---|---|---|---|
| **Observe** | 当前 ReActState | `observation: str`（结构化观察摘要） | `state.observations.append(observation)` |
| **Think** | observation + question + action_history | `action: {type, input, reasoning}` | `state.thoughts.append(reasoning)`, `state.current_action = action` |
| **Act** | action | `action_result: {success, data, error}` | 根据action_type更新对应字段（retrieved_docs/search_results/tool_results/generation） |

**具体示例**：
```
Step 1: 
  Observe → "用户问题: 量子计算最新进展, 尚未检索任何文档"
  Think → {"thought": "需要先检索知识库", "action": "retrieve", "action_input": {"query": "量子计算最新进展"}}
  Act → 检索到3篇文档

Step 2:
  Observe → "已检索到3篇文档, 信息可能不够全面"
  Think → {"thought": "知识库信息不足,需要搜索互联网", "action": "web_search", "action_input": {"query": "量子计算最新进展2025"}}
  Act → 搜索到2条结果

Step 3:
  Observe → "已检索3篇文档+2条搜索结果, 信息足够"
  Think → {"thought": "信息足够,生成回答", "action": "generate", "action_input": {}}
  Act → 生成回答

Step 4:
  Observe → "已生成回答(长度: 500)"
  Think → {"thought": "回答完整", "action": "finish", "action_input": {}}
  → 循环结束
```

**与 DAG 的关键区别**：DAG 中路径是预先确定的（所有人走同一条路），ReAct 中每一步的 action 都由 LLM 基于当前观察自主决定（不同问题走不同路径）。

---

### 70. DAG 节点如何拆解为 ReAct 工具 [权重: 9]

**问题**：你把 DAG 的节点拆解成了什么？哪些删除了？哪些合并了？

**参考答案**：

**拆解方案**：

| DAG 节点 | 处理方式 | 去向 |
|---------|---------|------|
| `intent_classification` | **删除** | 意图识别不再需要独立节点，Agent 在 Think 阶段自然理解意图 |
| `query_rewrite` | **封装为 Tool** | ToolRegistry: `query_rewrite_tool`，Agent 自主决定是否需要改写 |
| `retrieval` | **封装为 Tool** | ToolRegistry: `vector_search_tool`，Agent 自主决定是否检索 |
| `rerank` | **合并到 retrieval Tool 内部** | 检索后自动重排，不需要独立节点 |
| `tool_call` | **删除** | 不再需要独立节点，所有工具统一注册到 ToolRegistry |
| `generation` | **保留为特殊 Action** | `generate` 是 Agent 的核心 action |
| `evaluation` | **重构为 Observer** | 不再是独立节点，而是 Observe 阶段的一部分 |
| `reflection` | **删除** | 反思变成 Agent 的自主行为（Observe发现不好→Think决定换策略→Act换策略） |
| `web_search` | **封装为 Tool** | ToolRegistry: `web_search_tool`，Agent 自主决定是否搜索 |

**ToolRegistry 设计**：
```python
class ToolRegistry:
    """统一管理所有工具的注册、发现和执行"""
    
    def register(self, name, tool, category):
        """注册工具（支持动态注册，扩展只需一行代码）"""
    
    def get_description(self) -> str:
        """获取工具描述（供ReAct prompt使用）"""
    
    async def execute(self, name, args) -> Any:
        """执行工具调用（统一入口，带错误处理）"""
```

**扩展性对比**：
- DAG 模式：加一个工具 = 加节点 + 改路由 + 改边 + 改 state
- ReAct 模式：加一个工具 = `tool_registry.register("new_tool", NewTool())` 一行代码

---

### 71. Agent 防崩设计 [权重: 9]

**问题**：Agent 有自主决策能力后，如何防止它失控？比如无限循环、幻觉决策？

**参考答案**：

**三层防护机制**：

**第一层：SafetyPolicy（计数器限制）**
```python
class SafetyPolicy:
    MAX_STEPS = 10              # 最大步数
    MAX_TOOL_CALLS = 5          # 单轮最大工具调用次数
    MAX_RETRIEVE_CALLS = 3      # 最大检索次数
    MAX_GENERATE_CALLS = 2      # 最大生成次数
    MAX_CONSECUTIVE_FAILS = 3   # 最大连续失败次数
    
    def check(self, action) -> bool:
        """检查行动是否被允许，超限则拒绝"""
        if self.step_count > self.MAX_STEPS:
            return False  # 强制转为generate
```

**第二层：HallucinationGuard（幻觉检测）**
```python
class HallucinationGuard:
    def validate_action(self, action, state) -> Action:
        """检测不合理的决策并修正"""
        # 1. 工具不存在 → 改为generate
        # 2. 重复执行相同行动 → 改为generate（避免死循环）
        # 3. 有未用文档仍检索 → 改为generate（先利用已有信息）
```

**第三层：Error Signal 回流**
```python
# 错误信息保存在state.errors中，Agent在Observe阶段能看到
state["errors"].append("步骤3: tool_call失败 - 工具不存在")
# Agent在Think阶段会基于错误信息做出更明智的决策
```

**防崩场景示例**：
| 场景 | 防护机制 | 效果 |
|------|---------|------|
| Agent 反复检索同一查询 | SafetyPolicy MAX_RETRIEVE_CALLS=3 | 第4次检索被拒绝，强制生成 |
| Agent 调用不存在的工具 | HallucinationGuard 检测 | 自动修正为 generate |
| Agent 连续3次行动失败 | SafetyPolicy MAX_CONSECUTIVE_FAILS | 强制生成回答 |
| Agent 陷入死循环 | SafetyPolicy MAX_STEPS=10 | 第11步强制终止 |

---

### 72. 流式改造：从伪流式到真流式 [权重: 9]

**问题**：你的流式输出从伪流式改成了真流式，具体怎么改的？为什么 DAG 模式难以实现真流式？

**参考答案**：

**DAG 模式为什么难以实现真流式**：

三层原因：
1. **API 层**：`llm.invoke()` 同步阻塞，必须等待完整响应
2. **节点层**：LangGraph 的节点是原子操作——要么完成，要么不完成，没有中间状态
3. **架构层**：DAG 节点粒度太粗，无法在节点执行过程中向外部 yield 数据

**DAG 的伪流式实现**：
```python
# 先拿到完整文本，再分段yield模拟流式
generation_text = state.get("generation", "")  # 已经是完整文本了
for i in range(0, len(cleaned_text), 50):
    segment = cleaned_text[i:i+50]
    yield {"type": "chunk", "content": segment}
    await asyncio.sleep(0.02)  # 模拟流式延迟
# 用户感知：等待很久 → 突然快速输出
```

**ReAct 的真流式实现**：
```python
async def _stream_generate(self, state):
    """使用llm.astream()实现真正的逐token流式"""
    prompt = self._build_prompt(state)
    async for chunk in self.llm.astream(prompt):  # 关键：astream而非invoke
        token = chunk.content
        yield token  # 每个token立即输出
# 用户感知：实时逐字输出
```

**流式与 Agent 循环的结合**：
```
传统DAG流式:  [node1完成] → yield状态 → [node2完成] → yield状态 → ...
                    ↑ 粒度太粗，用户等太久

ReAct流式:    [observe] → yield thought → [think] → yield action →
              [act:generate] → yield token × N → [observe] → yield thought → ...
                    ↑ 每个阶段都有输出，用户始终能看到进展
```

**SSE 事件类型**：
| 事件类型 | 含义 | DAG 模式 | ReAct 模式 |
|---------|------|---------|-----------|
| `thought` | Agent 的观察结果 | ❌ | ✅ |
| `action` | Agent 选择的行动 | ❌ | ✅ |
| `observation` | 行动执行结果 | ❌ | ✅ |
| `token` | 逐 token 输出 | ❌ | ✅（真流式） |
| `chunk` | 分段输出 | ✅（伪流式） | ❌ |
| `done` | 完成信号 | ✅ | ✅ |

---

### 73. ReAct Agent 的状态管理 [权重: 8]

**问题**：ReAct Agent 的状态管理和 DAG 的状态管理有什么区别？

**参考答案**：

**DAG 状态（AgentState）**：节点输出管道
- 每个节点写入固定字段，下一个节点读取
- 状态是"数据流"，不是"决策依据"
- 例如：retrieval_node 写入 `retrieved_docs`，rerank_node 读取 `retrieved_docs`

**ReAct 状态（ReActState）**：决策上下文
- Agent 基于完整历史自主决策，state 是决策依据
- 核心新增字段：`observations`（观察历史）、`actions`（行动历史）、`errors`（错误信号）

```python
class ReActState(TypedDict):
    question: str
    
    # ReAct循环状态（DAG没有的）
    observations: List[str]       # 观察历史
    actions: List[Dict]           # 行动历史（含结果）
    current_step: int             # 当前步骤
    
    # 工作记忆（与DAG相同）
    retrieved_docs: List[Document]
    search_results: List[Document]
    tool_results: Dict[str, Any]
    
    # 错误信号（DAG没有的，供Agent感知失败）
    errors: List[str]
    warnings: List[str]
```

**关键区别**：DAG 的 state 是"流水线"（上游写入、下游读取），ReAct 的 state 是"黑板"（Agent 每步都读取全部信息做决策）。

---

### 74. 双模式架构的兼容性设计 [权重: 8]

**问题**：你同时保留了 DAG 和 ReAct 两种模式，怎么保证兼容性？

**参考答案**：

**兼容性设计原则**：上层 API 代码无需修改即可使用两种模式

**1. Graph 层统一接口**：
```python
class AgenticRAGGraph:
    def __init__(self, ..., use_react: bool = True):
        if use_react:
            self._react_agent = ReActAgent(...)
        else:
            self.graph = self._build_graph()
    
    def invoke(self, question, **kwargs):
        if self.use_react:
            return self._react_agent.invoke(question, **kwargs)
        else:
            # DAG模式原有逻辑
    
    async def stream_invoke(self, question, **kwargs):
        if self.use_react:
            async for event in self._react_agent.stream_run(...):
                yield event
        else:
            # DAG模式原有逻辑
```

**2. 输出格式统一**：
```python
# ReAct Agent 的 _format_result() 返回与 DAG 相同的结构
return {
    "answer": answer,
    "generation": generation,
    "sources": all_docs,
    "reranked_docs": retrieved_docs,
    "evaluation": {...},
    "intent": "react_agent",
    ...
}
```

**3. 配置化切换**：
```python
# settings.py
agent_mode: str = "react"  # "react" 或 "dag"

# 请求级动态切换
# API请求中可传 mode="dag" 临时切换到DAG模式
```

**4. 前端适配**：
- ReAct 模式新增 `thought`/`action`/`observation`/`token` 事件类型
- 前端对未知事件类型做容错处理，不崩溃
- DAG 模式的 `chunk` 事件和 ReAct 模式的 `token` 事件都正确显示

---

### 75. 项目难点（更新版） [权重: 8]

**问题**：重构过程中遇到的最大难点是什么？

**参考答案**：

1. **DAG 循环的拆解**：evaluation→reflection 循环看似是 Agent 行为，实则是伪循环。拆解后需要确保 ReAct 循环能自然实现相同功能且效果更好。

2. **流式改造**：从 `llm.invoke()` 改为 `llm.astream()` 不是简单替换，需要重新设计整个流式输出架构——DAG 的节点级流式和 ReAct 的阶段级流式完全不同。

3. **安全策略调参**：`MAX_STEPS`、`MAX_RETRIEVE_CALLS` 等阈值需要根据实际场景调优——太小会限制 Agent 能力，太大会导致资源浪费。

4. **双模式兼容**：保留 DAG 模式作为回退，同时确保 ReAct 模式的输出格式与上层 API 兼容，不破坏现有功能。

5. **模型名称大小写问题**：阿里云百炼 API 的模型名大小写敏感（`Qwen3.5-plus` vs `qwen3.5-plus`），导致 404 错误。修复方式：统一从环境变量读取，确保与 API 文档一致。

---

## 🔥 Agent 性能深度优化（第二轮优化，权重 9-10）

> 以下问题基于第二轮性能优化的实际经验，核心思路是"减少LLM调用次数"而非简单优化代码。

### 76. Agent 性能瓶颈分析 [权重: 10]

**问题**：你的 Agent 系统响应慢，你怎么定位瓶颈？哪些步骤最耗时？

**参考答案**：

**性能拆解方法**：按节点量化每个步骤的LLM调用次数和耗时

**DAG模式最坏路径延迟拆解（multi_hop + reflection + CRAG）**：
```
总延迟 = intent(1.5s) + query_rewrite(4.5s) + retrieval(0.7s) + rerank(1s) 
       + generation(5s) + evaluation(0.05s) + web_search(2s) + generation(5s) 
       + evaluation(0.05s) + reflection(3s) + evaluation(0.05s)
       ≈ 22.85s
总LLM调用: 5-8次
```

**Top 3 瓶颈**：

| 排名 | 瓶颈 | 原因 | 占总延迟比 |
|------|------|------|-----------|
| 1 | query_rewrite(strategy="all") | 串行调用2-3次LLM（expansion+decomposition），大多数查询不需要同时用两种策略 | DAG模式20-25% |
| 2 | generation | 输入context可达8000 tokens，输出800+ tokens | 25-35% |
| 3 | ReAct Think循环 | 每步都要LLM决策，即使下一步行动显而易见 | ReAct模式30-40% |

**关键洞察**：性能问题不是代码效率，而是**LLM调用次数过多**。DAG模式最坏路径需要5-8次LLM调用，ReAct模式需要3次以上。

---

### 77. 合并LLM调用：意图识别+查询改写 [权重: 10]

**问题**：你如何减少LLM调用次数？举个具体的合并例子。

**参考答案**：

**优化A：合并intent_classification + query_rewrite为单次LLM调用**

**优化前**（3-4次LLM调用）：
```
intent_classification(LLM) → route → query_rewrite(LLM×2-3) → retrieval
```

**优化后**（1次LLM调用）：
```
intent_and_rewrite(LLM×1) → route → retrieval
```

**实现方式**：设计一个合并prompt，同时输出意图和改写查询：
```python
COMBINED_PROMPT = """你是一个查询分析助手，需要同时完成两个任务：
1. 意图分类：将问题归类为 factual/reasoning/summary/multi_hop/tool_call
2. 查询改写：仅对multi_hop和reasoning类型，生成2-3个不同表述的查询
用户问题：{question}
请以JSON格式返回：{{"intent": "xxx", "rewrites": ["query1", "query2"]}}"""
```

**收益**：减少2-3次LLM调用，节省3-5秒

**为什么这样合并是安全的**：
- 意图识别和查询改写都是对用户问题的"理解"阶段，输入相同
- 合并后LLM只需一次推理就能同时完成两个任务
- 对于factual/summary/tool_call类型，rewrites为空列表，不影响后续流程

---

### 78. ReAct快速路径：跳过不必要的Think [权重: 9]

**问题**：ReAct Agent每步都要LLM决策，如何减少不必要的Think调用？

**参考答案**：

**优化B：对确定性步骤跳过Think，直接执行**

**核心洞察**：ReAct循环中有些步骤是"显而易见"的，不需要LLM决策：
- 首步且无缓存 → 必定检索，不需要LLM决策
- 已有检索结果（≥2篇）且未生成 → 必定生成，不需要LLM决策

**实现方式**：
```python
def _should_skip_think(self, state, step) -> Optional[Action]:
    """快速路径判断：对确定性步骤跳过LLM Think调用"""
    # 首步且无检索结果：直接检索
    if step == 0 and not state.get("retrieved_docs"):
        return Action(type="retrieve", input={"query": state["question"]},
                      reasoning="首步直接检索，跳过LLM决策")
    # 已有检索结果且未生成：直接生成
    if state.get("retrieved_docs") and len(state["retrieved_docs"]) >= 2 and not state.get("generation"):
        return Action(type="generate", input={},
                      reasoning="已有充足检索结果，直接生成回答")
    return None  # 需要LLM决策
```

**优化前后对比**：
```
优化前（3次LLM调用）：Think1(retrieve) → retrieve → Think2(generate) → generate
优化后（2次LLM调用）：快速路径(retrieve) → retrieve → Think(generate) → generate
```

**收益**：典型2步流程中减少1-2次Think调用，节省1.5-3秒

**为什么不影响质量**：跳过的都是确定性决策，LLM在这些步骤上的决策结果和快速路径完全一致，只是省掉了LLM调用的开销。

---

### 79. 规则预过滤意图 + 快速退出 [权重: 8]

**问题**：除了合并LLM调用，还有哪些方式减少调用次数？

**参考答案**：

**优化D：规则预过滤意图——常见模式直接匹配，跳过LLM**

```python
def _rule_based_intent(question: str) -> Optional[str]:
    """规则匹配常见意图，避免LLM调用"""
    if re.search(r'(今天|现在|最新|当前|实时)', question):
        return "tool_call"       # 实时信息类
    if re.search(r'(总结|概括|梳理|归纳)', question):
        return "summary"          # 总结类
    if len(question) < 20 and re.search(r'(什么是|什么叫|多少)', question):
        return "factual"          # 短问题+疑问词
    return None                   # 需要LLM判断
```

**收益**：对常见问题节省1次LLM调用（1.5秒）

**优化C：generation快速退出——高质量答案跳过evaluation+reflection**

```python
def should_skip_evaluation(state) -> bool:
    """高质量答案跳过评估直接结束"""
    answer = state.get("generation", "")
    docs = state.get("reranked_docs", [])
    # 答案够长且有检索文档支撑 → 跳过评估
    if len(answer) > 100 and len(docs) >= 2:
        return True
    return False
```

**收益**：对高质量答案跳过evaluation+reflection，节省0-5秒

**优化F：弱化reflection——默认关闭，反思后直接END**

原来reflection最多2轮，每轮1次LLM调用。优化后：
- `max_reflection_steps` 默认改为0（仅在用户显式请求时启用）
- reflection后直接END，不再回evaluation循环（避免reflection→evaluation→reflection无效循环）

**收益**：减少0-2次LLM调用，节省0-6秒

---

### 80. 并行化与流式优化 [权重: 8]

**问题**：哪些串行步骤可以并行化？流式输出怎么优化？

**参考答案**：

**优化G：记忆并行加载**

原来短期记忆和长期记忆串行加载，改为`asyncio.gather()`并行：
```python
# 优化前：串行
await self._load_short_term_memory(state, session_id)
await self._search_long_term_memory(state, user_id, question)

# 优化后：并行
await asyncio.gather(
    self._load_short_term_memory(state, session_id),
    self._search_long_term_memory(state, user_id, question)
)
```
**收益**：节省0.5-1秒

**优化H：压缩ReAct System Prompt**

原来每步Think都发送完整prompt（~800 tokens），压缩后：
```python
# 优化前：详细prompt ~800 tokens/步
REACT_SYSTEM_PROMPT = """你是一个智能AI助手，通过观察-思考-行动的循环..."""

# 优化后：压缩prompt ~300 tokens/步
REACT_SYSTEM_PROMPT_COMPACT = """AI助手。工具:{tools_description}
行动:retrieve(query)/web_search(query)/generate/finish
规则:先检索→不够则搜索→足够则生成→完成则结束
状态:问题={question} 步骤={step_count} 信息={information_summary}
输出JSON:{{"thought":"...","action":"...","action_input":{{...}}}}"""
```
**收益**：每步减少~500 tokens输入，10步累计节省5000 tokens

**优化I：query_rewrite并行化**

原来expansion和decomposition串行执行，改为并行：
```python
async def arewrite(self, query, strategy="all"):
    expansion_task = loop.run_in_executor(None, self.expansion.expand, query)
    decomposition_task = loop.run_in_executor(None, self.decomposer.decompose, query)
    expansion_results, decomposition_results = await asyncio.gather(
        expansion_task, decomposition_task
    )
```
**收益**：从串行4.5s→并行2.5s，节省2秒

---

### 81. 优化前后对比与效果 [权重: 9]

**问题**：你的第二轮优化整体效果如何？哪些改动收益最大？

**参考答案**：

**DAG模式优化前后对比**：
```
优化前（multi_hop最坏路径，5-8次LLM调用）：
用户输入 → intent(LLM) → rewrite(LLM×2-3) → retrieval → rerank 
→ generation(LLM) → evaluation → [reflection(LLM)] → [CRAG循环] → END
总LLM调用: 5-8次 | 总延迟: 15-23s

优化后（同路径，2-3次LLM调用）：
用户输入 → intent+rewrite合并(LLM×1) → retrieval → rerank 
→ generation(LLM) → [快速退出或evaluation] → END
总LLM调用: 2-3次 | 总延迟: 6-10s
```

**ReAct模式优化前后对比**：
```
优化前（典型3步，3次LLM调用）：
Think1(LLM) → retrieve → Think2(LLM) → generate(LLM) → END
总LLM调用: 3次 | 总延迟: ~8s

优化后（快速路径2步，2次LLM调用）：
快速路径(retrieve) → Think(LLM) → generate(LLM) → END
总LLM调用: 2次 | 总延迟: ~6.5s
```

**综合预期效果**：

| 指标 | 优化前 | 优化后 | 改善幅度 |
|------|--------|--------|---------|
| DAG模式LLM调用次数 | 5-8次 | 2-3次 | **-50%~70%** |
| DAG模式总延迟 | 15-23s | 6-10s | **-50%~60%** |
| ReAct模式LLM调用次数 | 3次 | 2次 | **-33%** |
| ReAct模式总延迟 | ~8s | ~6.5s | **-20%** |
| Token消耗 | ~6000/请求 | ~3000/请求 | **-50%** |

**收益最大的3个改动**：
1. 🏆 **合并intent+rewrite为单次LLM调用**（优化A）—— 一次改动减少2-3次LLM调用，节省3-5s
2. 🏆 **ReAct快速路径**（优化B）—— 对ReAct模式减少1-2次Think调用，节省1.5-3s
3. 🏆 **弱化reflection+快速退出**（优化C+F）—— 默认关闭reflection，高质量答案跳过evaluation，节省0-6s

**优化优先级原则**：
- **减少LLM调用次数** > 减少token消耗 > 代码级优化
- 如果一个优化不能减少调用次数或token消耗，优先级降低
- 避免过度工程优化（如复杂微服务），避免理论优化但无法落地

---

### 82. 缓存不生效排查：缓存键不一致Bug [权重: 10]

**问题**：你实现了生成结果缓存，但重复问同一个问题，缓存从来不命中。你怎么排查？

**参考答案**：

**排查过程**：
1. 确认缓存写入逻辑正常：检查`gen_cache.set()`是否被调用，日志显示"已保存"
2. 确认缓存读取逻辑正常：检查`gen_cache.get()`是否被调用，参数是什么
3. **对比缓存键生成逻辑**：发现写入和读取用的键不同

**发现的Bug 1：DAG模式缓存键不一致**

```python
# graph.py 首次快速路径检查
cached_gen = self.gen_cache.get(question, None)  # 键 = md5(question.lower())

# graph.py generation节点写入
self.gen_cache.set(question, cleaned_text, intent=state.get("intent"))  # 键 = md5(question.lower() + "|factual")
```

**根因**：`GenerationCache` 的 `_generate_key()` 方法中，`intent` 参数参与键生成。读取时 `intent=None`，写入时 `intent="factual"`，两个键的MD5不同，永远命中不了。

**修复方案**：双键写入 + 回退兼容
- `set()` 时同时写入 `(question, intent)` 和 `(question, None)` 两个键
- `get()` 时优先精确匹配 `(question, intent)`，回退查找 `(question, None)`
- 实际 intent 信息存入 metadata 而非参与键生成
```python
# set 时双键写入
self._cache[key_with_intent] = (value, timestamp)
self._cache[key_without_intent] = (value, timestamp)

# get 时双重检查
key = self._generate_key(question, intent)  # 优先精确匹配
if key in self._cache and self._is_valid(key):
    return self._cache[key][0]
# 回退查找：兼容老缓存（写入时只有 None 键）
key_fallback = self._generate_key(question, None)
if key_fallback in self._cache and self._is_valid(key_fallback):
    return self._cache[key_fallback][0]
```

**发现的Bug 2：ReAct模式完全没有缓存**

**根因**：系统默认使用 ReAct 模式（`use_react=True`），走的是 `react_agent.py` 的 `stream_run` 方法，而缓存逻辑只写在了 `graph.py` 的 DAG 模式中。ReAct 模式完全绕过了缓存代码。

**修复方案**：
- 在 `stream_run` 循环开始前加入缓存检查（命中则直接返回缓存结果）
- 在生成完成后加入缓存写入

**缓存键设计最佳实践**：
1. 缓存键的生成逻辑必须在读取和写入时完全一致
2. 如果需要在键中包含多个维度，读取和写入都要传相同维度
3. 不用于区分缓存项的维度信息，应该放在 metadata 中，而不是参与键生成
4. **生产环境调试技巧**：在 set/get 时打印生成的键（MD5前8位），直观对比是否一致

---

### 83. 双模式系统缓存设计陷阱 [权重: 8]

**问题**：你的系统有DAG和ReAct两种执行模式，缓存应该怎么设计？

**参考答案**：

**陷阱**：只在其中一种模式中实现缓存，另一种模式绕过了缓存。

**正确做法**：

| 设计要点 | 说明 |
|---------|------|
| 缓存层与执行层解耦 | 缓存检查应该在两种模式的入口都执行，不应该依赖具体执行路径 |
| 统一的缓存接口 | 无论哪种模式，都通过同一个缓存实例的同一个方法存取 |
| 缓存键只依赖用户输入 | 不依赖执行模式、意图等可变因素，确保跨模式缓存共享 |
| **双键写入+回退兼容** | `set()` 时同时写入 `(question,intent)` 和 `(question,None)` 两个键，`get()` 时优先精确匹配，回退查找老缓存 |
| 跨模式缓存共享 | DAG模式和ReAct模式对同一问题的回答应该可以互相复用 |

**实际案例**：
```python
# 错误做法：只在DAG模式检查缓存
# graph.py stream_invoke 中检查缓存
if cached_gen: return cached_gen

# ReAct模式 stream_run 中没有检查缓存 -> 永远不会命中

# 正确做法：两种模式都检查缓存
# graph.py stream_invoke -> 检查缓存
# react_agent.py stream_run -> 检查缓存（已修复）
```

**经验教训**：当系统有多条执行路径时，缓存应该放在所有路径的公共入口点，或者在每条路径上都独立实现。遗漏任何一条路径都会导致"缓存看起来写了但读不到"的诡异Bug。
