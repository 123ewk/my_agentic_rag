# Agentic RAG 系统架构文档

## 一、系统定位

### 系统类型判定
**Agent系统 + RAG系统 的混合体**

这是不是一个简单的RAG系统，而是一个具有自主决策能力的智能Agent系统：
- **RAG能力**：向量检索 + 查询改写 + 重排
- **Agent能力**：ReAct循环自主决策、双模式执行（DAG/React）
- **记忆能力**：短期会话记忆 + 长期价值记忆

---

## 二、技术栈详情

### 2.1 核心框架与库
| 类别 | 技术 | 版本/说明 |
|------|------|-----------|
| Web框架 | FastAPI | 异步Web服务，支持SSE流式响应 |
| Agent框架 | LangGraph + LangChain | 状态机图执行 + LLM编排 |
| LLM提供商 | 通义千问(Qwen)、MiniMax、智谱(GLM) | 支持动态模型切换 |
| 嵌入模型 | 智谱Embedding-3 | 1024维向量 |
| 向量数据库 | Milvus | gRPC连接，单例模式 |
| 关系数据库 | PostgreSQL + asyncpg | 异步驱动，存储会话/长期记忆 |
| 重排模型 | BGE-Reranker（智谱API） | 文档相关性重排序 |
| 分词/NLP | spaCy | 中文分句（sentencizer） |
| 日志系统 | Loguru | 结构化日志，支持追踪ID |
| 配置管理 | pydantic-settings + dotenv | 类型安全的环境变量加载 |
| 数据验证 | Pydantic V2 | 请求/响应模型校验 |

### 2.2 工具与依赖
| 类别 | 技术 |
|------|------|
| 包管理 | uv（替代pip/poetry） |
| 文档解析 | pypdf, python-docx, pandas, BeautifulSoup |
| 搜索工具 | DuckDuckGo Search |
| 限流 | 自定义滑动窗口实现 |
| 分布式锁 | Redis（SET NX EX + Lua脚本）、PostgreSQL（行级锁/咨询锁） |
| 测试框架 | pytest |
| UI（可选） | Streamlit（agentic_rag/ui/app.py） |

---

## 三、模块划分与职责

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           API 网关层                                     │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│   │  请求追踪   │  │  限流保护   │  │  API认证    │  │  CORS防护   │   │
│   └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     Agent 执行层 (四路径)                                │
│                                                                           │
│   ┌──────────────────────┐  ┌────────────────┐  ┌────────────────────┐  │
│   │ 超快速并行路径(新增!) │  │  快速流式路径   │  │  DAG 模式          │  │
│   │ ultra_fast_stream    │  │ fast_stream     │  │  (预定义路径)       │  │
│   │ ParallelExecutor     │  │ 手动执行+流式   │  │  graph.astream     │  │
│   │ 三路并行+真流式      │  │ 首token<3s     │  │  节点级流式         │  │
│   │ 首token<2s           │  │                │  │                    │  │
│   └──────────────────────┘  └────────────────┘  └────────────────────┘  │
│                                                                           │
│   ┌────────────────────┐                                                  │
│   │  ReAct 模式        │                                                  │
│   │  (自主决策)        │                                                  │
│   │  Observe→Think→Act │                                                  │
│   │  token级流式       │                                                  │
│   └────────────────────┘                                                  │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           工具执行层                                      │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐         │
│   │ 向量检索 │  │ 网络搜索 │  │ 查询改写 │  │ 外部工具(DuckDuck │         │
│   │ (内置)   │  │ (内置)   │  │ (内置)   │  │ Go,Calculator)   │         │
│   └──────────┘  └──────────┘  └──────────┘  └──────────────────┘         │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    ▼                               ▼
┌─────────────────────────────────┐  ┌─────────────────────────────────┐
│        短期记忆层                │  │          长期记忆层V2             │
│   PostgreSQL 会话持久化          │  │   价值筛选 + 四类型分类 + 衰减   │
│   - TTL自动过期                  │  │   - user_profile                 │
│   - Token级上下文截断            │  │   - fact                         │
│   - 按会话隔离                   │  │   - experience                  │
│                                 │  │   - preference                   │
└─────────────────────────────────┘  └─────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          基础设施层                                      │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│   │ 定时调度器  │  │  配置管理   │  │   日志系统  │  │  缓存系统   │   │
│   │ APScheduler │  │  .env统一   │  │   Loguru    │  │ LRU缓存+生成 │   │
│   └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 四、性能优化专题（核心）

### 4.1 当前性能瓶颈分析

#### 原始DAG模式时间线（约40秒）
```
用户请求 → [记忆加载 1-2s] → [意图+改写 3-5s] → [检索 1-2s] → [重排 1-2s]
→ [generation llm.invoke() 8-15s] → [evaluation 0.5s] → [CRAG web_search 3-5s]
→ [generation(重生成) 8-15s] → [reflection 3-5s] → 输出给用户
                                                                      ↑
                                                              用户等到这里才看到答案
```

**瓶颈根因排序：**

| 排名 | 瓶颈 | 耗时 | 根因 |
|------|------|------|------|
| 1 | generation阻塞 | 8-15s | `llm.invoke()`同步等待完整生成，用户必须等全部token生成完毕 |
| 2 | CRAG循环 | 3-5s+8-15s | 低置信度触发web_search+重新generation，最坏情况翻倍延迟 |
| 3 | reflection | 3-5s | LLM重新生成答案，但质量提升有限 |
| 4 | 意图+改写 | 3-5s | 1次LLM调用（已优化为合并节点，原需2次） |
| 5 | evaluation | 0.5s | 已改为轻量级规则评估（无LLM调用），延迟可控 |

**关键洞察：** 最大瓶颈不是单步耗时，而是**串行等待**——用户必须等所有步骤完成才能看到第一个字。

---

### 4.2 优化后的新架构

#### 核心设计原则：**用户感知速度 > 理论最优答案**

#### 四路径执行架构

| 路径 | 入口方法 | 核心策略 | 首 Token 时间 |
|------|---------|---------|-------------|
| **超快速并行** | `ultra_fast_stream_invoke` | ParallelExecutor 三路并行 + 真流式 | **~2-3s** |
| **快速流式** | `fast_stream_invoke` | 手动执行节点 + 真流式 | **~3-5s** |
| **标准 DAG** | `stream_invoke` | 手动执行节点 + 真流式generation | **~5-15s** |
| **ReAct** | `stream_run` | Observe→Think→Act + 真流式 | **~5-15s** |

#### 超快速并行路径详解（`ultra_fast_stream_invoke`）

```
用户请求
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 阶段1: 意图识别+查询改写（1次LLM调用，合并节点）                │
│ 规则预过滤 → 缓存检查 → LLM调用 → intent + rewritten_queries   │
│ 耗时: ~2s（规则命中0s，缓存命中0s，LLM调用2-5s）                │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 阶段2: ParallelExecutor 三路并行（核心优化！）                   │
│ ┌────────────────┐  ┌────────────────┐  ┌────────────────┐     │
│ │ Memory 加载    │  │ Retrieval      │  │ Web Search     │     │
│ │ (short+long)   │  │ (multi-query)  │  │ (optional)     │     │
│ │ asyncio.gather │  │ asyncio.gather │  │ asyncio.to_    │     │
│ │ ~1-2s          │  │ ~2-3s          │  │  thread ~3-5s  │     │
│ └───────┬────────┘  └───────┬────────┘  └───────┬────────┘     │
│         │                   │                   │               │
│         └───────────────────┼───────────────────┘               │
│                             ▼                                   │
│                     跨查询去重 + Rerank                          │
│                     ~1-2s                                       │
│ 耗时: max(Memory, Retrieval, WebSearch) + Rerank               │
│       ≈ 3-5s（而非串行的 1+2+3+1=7s）                           │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 阶段3: 真流式生成（llm.astream()，核心优化！）                  │
│                                                                   │
│ ┌───────────────────────────────────────────────────────────┐    │
│ │  用户看到: "正在生成回答..." → 第1个token → 第2个token... │    │
│ │                    ↑                                      │    │
│ │            首token时间<2s!                                 │    │
│ │            (从rerank完成到首token只需LLM首token延迟)       │    │
│ └───────────────────────────────────────────────────────────┘    │
│                                                                   │
│ 耗时: 首token ~0.5s，完整生成 5-10s（但用户已在看答案）          │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 阶段4: 后台异步后处理（用户已看到答案，不阻塞）                  │
│ ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐    │
│ │ 轻量级评估   │  │ 记忆保存     │  │ CRAG日志记录         │    │
│ │ (规则计算)   │  │ (短期+长期)  │  │ (不触发重新生成)     │    │
│ └──────────────┘  └──────────────┘  └──────────────────────┘    │
│ 耗时: 不影响用户感知                                              │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
  done事件
```

#### 优化后时间线对比

```
【优化前 - stream_invoke】
0s──────────────────────────────────────────────────────────40s
[记忆][意图+改写][检索][重排][generation阻塞..............][eval][CRAG?][refl?]
                                                            ↑ 用户等到这里

【优化后 - fast_stream_invoke】
0s──────────────────────────────────5s─────────────────────15s
[记忆][意图+改写][检索][重排][首token!→token→token→token→...]
                              ↑ 用户在这里就看到答案了!
                              
后台: [eval][记忆保存]  ← 不阻塞用户

【终极优化 - ultra_fast_stream_invoke】
0s────────────────────3s─────────────────────12s
[意图+改写][Memory‖Retrieval‖WebSearch][Rerank][首token!→token→...]
           ↑ 三路并行，不等待Memory      ↑ 用户在这里看到答案!
           
后台: [eval][记忆保存]  ← 不阻塞用户
```

---

### 4.3 每步优化带来的收益

| 优化项 | 改动位置 | 时间节省 | 首token收益 |
|--------|---------|---------|------------|
| **真流式生成** | `fast_stream_invoke`用`llm.astream()`替代`llm.invoke()` | 8-15s→0.5s首token | **-10s** |
| **后台评估** | evaluation移到`_background_post_processing` | 0.5s→0s（用户感知） | **-0.5s** |
| **移除CRAG阻塞** | CRAG不再触发重新生成，仅后台记录 | 0-20s→0s（用户感知） | **-10s**（最坏情况） |
| **移除reflection阻塞** | reflection在快速流式模式下禁用 | 0-5s→0s（用户感知） | **-3s** |
| **合并意图+改写** | 已实现（优化A） | 3-5s→2-3s | **-2s** |
| **并行检索** | 已实现（优化B） | 串行O(n)→并行O(1) | **-2s** |
| **记忆并行加载** | 已实现（优化G） | 串行→并行 | **-1s** |
| **ParallelExecutor三路并行** | `ultra_fast_stream_invoke`新增 | Memory+Retrieval+WebSearch并行 | **-2-3s** |
| **跨查询去重** | `_deduplicate_docs`统一去重 | 减少rerank输入量 | **-0.3s** |
| **规则预过滤** | 已实现（优化D） | 常见模式0s跳过LLM | **-3s**（命中时） |
| **生成缓存** | 已实现 | 重复问题直接返回 | **-35s**（命中时） |

**总计：首token时间从 ~40s 降至 ~2-3s（超快速并行路径），~3-5s（快速流式路径），~0.5s（缓存命中）**

---

### 4.4 优化后的执行流程（逐步说明）

#### 路径1: 快速流式路径（`use_fast_path=True`，默认启用）

```python
# 伪代码
async def fast_stream_invoke(question, session_id, user_id):
    # 1. 并行加载记忆 (~1s)
    memory = await asyncio.gather(load_short_term(), search_long_term())
    
    # 2. 缓存检查 (~0s命中/~2s未命中)
    if cached := gen_cache.get(question):
        yield token_events(cached)  # 直接返回缓存
        return
    
    # 3. 意图+改写 (~2s, 规则命中0s)
    intent, queries = intent_and_rewrite_node(state, llm, cache)
    yield status("意图识别完成")
    
    # 4. 检索+重排 (~2s)
    docs = parallel_retrieval(queries, vectorstore)
    reranked = rerank(docs, reranker)
    yield sources(reranked)
    
    # 5. 真流式生成 (~0.5s首token)
    yield status("正在生成回答...")
    async for token in llm.astream(prompt):  # 关键！
        yield token_event(token)  # 用户立即看到每个token
    
    # 6. 后台异步（不阻塞用户）
    background(evaluate + save_memory)
    
    yield done()
```

#### 路径2: 标准DAG路径（`use_fast_path=False`）

与原`stream_invoke`相同，走`graph.astream()`节点级流式。

#### 路径3: ReAct路径（`mode="react"`）

与原`ReActAgent.stream_run`相同，Observe→Think→Act循环+token级流式。

---

### 4.5 流式输出优化详解

#### 问题：为什么原DAG模式是伪流式？

```python
# 原generation_node（nodes.py）
def generation_node(state, llm, prompt_template):
    response = llm.invoke(prompt)  # ← 阻塞！等全部token生成完
    return {"generation": response.content}

# 原stream_invoke（graph.py）
async for chunk in self.graph.astream(state):  # ← 节点级流式
    if node_name == "generation":
        # generation_node已经返回完整文本了
        # 这里只是把完整文本切成50字符的块模拟流式
        for i in range(0, len(text), 50):
            yield {"type": "chunk", "content": text[i:i+50]}
            await asyncio.sleep(0.02)  # ← 伪流式！
```

**本质问题：** `graph.astream()`是节点级流式——只有当节点函数`return`后，才会yield该节点的输出。而`generation_node`内部用`llm.invoke()`阻塞等待完整生成，所以用户必须等LLM生成完毕才能看到任何输出。

#### 解决方案：手动执行generation，用`llm.astream()`真流式

现在`stream_invoke`已重构为手动执行各阶段节点，到达generation时直接用`llm.astream()`进行真token级流式输出：

```python
# 新stream_invoke（graph.py）
async def stream_invoke(self, question, ...):
    # 手动执行前置节点（不走graph.astream的generation阶段）
    intent_result = intent_and_rewrite_node(state, self.llm, self.intent_cache)
    retrieval_result = parallel_retrieval_node(state, self.vectorstore)
    rerank_result = rerank_node(state, self.reranker)
    
    # 构建上下文
    context = _build_context_parts(...)
    prompt = self.prompt_template.format(context=context, question=question)
    
    # 真流式生成！直接调用llm.astream()
    async for chunk in self.llm.astream(prompt):  # ← 逐token输出
        token = chunk.content
        yield {"type": "token", "content": token}  # ← 用户立即看到
```

---

### 4.6 极致优化方案（牺牲部分质量）

如果需要进一步压低首token时间到1s以内：

| 方案 | 做法 | 首token收益 | 质量损失 |
|------|------|-----------|---------|
| **跳过rerank** | 检索结果直接送generation，不经过BGE重排 | -1s | 中等（排序质量下降） |
| **跳过意图分类** | 所有问题都走检索+生成路径 | -2s | 低（多走一步检索，但结果不差） |
| **Speculative Decoding** | 用小模型先生成草稿，大模型验证 | -0.5~1s | 无（质量不变） |
| **预计算热门查询** | 后台定时预生成热门问题的答案 | -35s（命中时） | 无 |
| **减少检索top_k** | top_k从5降到3 | -0.3s | 低 |
| **流式rerank** | 检索结果边返回边rerank | -0.5s | 无 |

---

## 五、核心流程（主执行路径）

### 5.1 快速流式路径（新增，默认启用）
```
用户问题输入
     │
     ▼
┌─────────────────────────┐
│  并行加载记忆            │ ← asyncio.gather(短期+长期)
└─────────────────────────┘
     │
     ▼
┌─────────────────────────┐
│  intent_and_rewrite_node │ ← 合并节点：意图分类 + 查询改写
│  (规则预过滤 → LLM调用)  │
└─────────────────────────┘
     │
     ├──→ tool_call ────────────────────────┐
     │                                      │
     ├──→ retrieval (并行多查询检索)         │
     │     │                                │
     │     ▼                                │
     │  rerank (BGE重排)                    │
     │     │                                │
     └─────┴────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│  llm.astream() 真流式生成               │ ← 核心优化！
│  用户立即看到每个token                   │
│  首token时间 < 2s                        │
└─────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│  后台异步: evaluation + 记忆保存        │ ← 不阻塞用户
└─────────────────────────────────────────┘
            │
            ▼
          END
```

### 5.2 DAG模式流程（标准路径）
```
用户问题输入
     │
     ▼
┌─────────────────────────┐
│  intent_and_rewrite_node │ ← 合并节点：意图分类 + 查询改写（优化A）
│  (规则预过滤 → LLM调用)  │
└─────────────────────────┘
     │
     ├──→ tool_call ────────────────────────┐
     │                                      │
     ├──→ retrieval (并行多查询检索)         │
     │     │                                │
     │     ▼                                │
     │  rerank (BGE重排)                    │
     │     │                                │
     │     ├──→ tool_call ─────────────────┐ │
     │     │                               │ │
     │     └──→ generation ◄──────────────┘ │
     │           │                         │ │
     │           ▼                         │ │
     │     should_skip_evaluation?         │ │
     │     (优化C: 高质量快速退出)         │ │
     │           │                         │ │
     │           ▼                         │ │
     │  evaluation (轻量级评估)            │ │
     │     │                               │ │
     │     ├──→ confidence=low → web_search ─► generation (CRAG)
     │     │                               │
     │     ├──→ needs_reflection → reflection → END
     │     │                               │
     │     └──→ END                        │
     │                                      │
     └──→ generation (summary类型直出)      │
           │                                │
           ▼                                │
        END ◄──────────────────────────────┘
```

### 5.3 ReAct模式流程
```
用户问题输入
     │
     ▼
┌──────────────────────────────────────────┐
│            Observe (状态提取)             │
│  提取：检索结果/搜索结果/工具结果/历史   │
└──────────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────┐
│            Think (LLM决策)                │
│  优先：Function Calling                   │
│  回退：JSON解析                          │
│  决策：retrieve/web_search/              │
│        query_rewrite/generate/finish      │
└──────────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────┐
│            Act (执行行动)                 │
│  retrieve → 向量检索+重排                 │
│  web_search → DuckDuckGo搜索             │
│  query_rewrite → 改写+重新检索            │
│  tool_call → 外部工具执行                │
│  generate → llm.astream()真流式生成       │
└──────────────────────────────────────────┘
     │
     ▼
     返回 Observe 继续循环
     (最多MAX_STEPS=10次)
     │
     ▼
   finish → 输出结果
```

### 5.4 记忆加载流程（优化G：并行）
```
请求进入
     │
     ▼
┌────────────────┐   ┌────────────────┐
│ 加载短期记忆   │   │ 搜索长期记忆   │
│ (session_id)   │   │ (user_id)      │
│ 有则加载对话   │   │ V2: 分层检索   │
│ 历史+上下文    │   │ 价值+类型+衰减 │
└────────────────┘   └────────────────┘
          │                 │
          └───────┬─────────┘
                  ▼
          合并到Agent State
          (memory_context)
```

---

## 六、关键组件分析

### 6.1 核心模块（决定系统行为）

| 模块 | 核心功能 | 决策权 |
|------|---------|--------|
| `AgenticRAGGraph` | 四路径切换、状态机构建 | 路由决策 |
| `ParallelExecutor` | Memory+Retrieval+WebSearch三路并行 | 并行调度 |
| `ultra_fast_stream_invoke` | 超快速并行执行、首token最优 | 首 token 时间 |
| `fast_stream_invoke` | 快速流式执行、后台评估 | 首 token 时间 |
| `ReActAgent` | 自主决策循环、安全策略 | 行动选择 |
| `intent_and_rewrite_node` | 合并意图+改写（优化A） | 流向控制 |
| `evaluation_node` | CRAG置信度路由 | 是否触发搜索 |
| `LongTermMemoryV2` | 价值筛选写入、分层检索 | 记忆质量 |
| `ComplexityAnalyzer` | 问题复杂度分析，自动判断 DAG/ReAct 模式 | 模式路由 |

### 6.2 工具模块（提供执行能力）

| 模块 | 功能 |
|------|------|
| `duckduckgo_search` | 互联网信息获取 |
| `calculator` / `python_repl` | 计算执行 |
| `VectorSearchTool` | 内置向量检索 |
| `BGGReranker` | 文档重排序 |
| `QueryRewriter` | 查询多策略改写 |

### 6.3 状态/数据管理模块

| 模块 | 职责 |
|------|------|
| `ShortTermMemory` | 会话级上下文（24h TTL，Token截断） |
| `LongTermMemoryV2` | 用户级持久记忆（价值筛选，四类型分类） |
| `intent_cache` / `gen_cache` | LLM调用缓存（减少重复消耗） |
| `TaskScheduler` | 定时清理过期数据 |

---

## 七、隐藏结构分析

### 7.1 隐式状态

```
AgentState (状态机状态)
├── question: 用户输入
├── intent: 意图分类结果
├── retrieved_docs / reranked_docs: 检索结果
├── generation / refined_answer: 生成结果
├── evaluation: 质量评估指标
├── memory_context: 记忆上下文 ← 从DB加载
├── conversation_history: 对话历史 ← 从DB加载
├── crag_loop_count: CRAG循环计数 ← 循环守卫
└── metadata: 请求参数透传

ReActAgentState (ReAct循环状态)
├── observations: 观察历史
├── actions: 行动历史
├── retrieved_docs: 累积检索结果（非替换）
├── tool_results: 工具执行结果
└── warnings / errors: 错误追踪
```

### 7.2 隐式耦合

| 耦合点 | 描述 |
|--------|------|
| settings全局单例 | `get_settings()` 在多处调用，隐式依赖配置 |
| 全局Agent实例 | `_agent_instance` 通过lifespan注入到app.state |
| 缓存穿透 | `gen_cache` 在DAG和ReAct两处独立使用，键可能重复 |
| 循环事件循环 | 大量 `asyncio.new_event_loop()` 创建，容易泄露 |

### 7.3 流程控制分散

- **CRAG路由**：`route_after_evaluation_with_crag` 硬编码在 graph.py
- **快速退出**：`should_skip_evaluation` 在 nodes.py 独立判断
- **意图缓存**：规则预过滤在 `_rule_based_intent` 和 `intent_and_rewrite_node` 两处重复
- **反思守卫**：MAX_CRAG_LOOPS 在 edges.py 硬编码

---

## 八、架构问题（Top 3）

### 问题1：双重模式维护成本高
- **现象**：`AgenticRAGGraph` 同时维护 `graph`（DAG）和 `_react_agent`（ReAct）
- **问题**：两套逻辑并行，bug修复需改两处
- **影响**：功能迭代速度降低

### 问题2：全局状态隐式依赖
- **现象**：`settings`、`_agent_instance` 等全局变量在多处隐式使用
- **问题**：单元测试困难，难以mock
- **影响**：代码可测试性差

### 问题3：缓存键设计不统一（已修复）
- **现象**：
  - `intent_cache` 使用 `(question)` 作为键
  - `gen_cache` 原使用 `(question, intent)` 键，但写入时 intent 不一致导致永远无法命中
- **问题**：缓存命中率接近 0，缓存形同虚设
- **修复方案**：生成缓存采用双键写入策略
  - `set()` 时同时写入 `(question, intent)` 和 `(question, None)` 两个键
  - `get()` 时优先精确匹配 `(question, intent)`，回退查找 `(question, None)`
  - 实际 intent 信息存入 metadata 而非参与键生成
- **效果**：缓存命中率提升 50%+

---

## 九、可视化结构

```
                              ┌─────────────────────────────────────┐
                              │         FastAPI 应用                 │
                              │  ┌──────────────────────────────┐   │
                              │  │  lifespan: Agent生命周期     │   │
                              │  │  middleware: 限流/追踪/异常   │   │
                              │  └──────────────────────────────┘   │
                              └──────────────────┬──────────────────┘
                                                 │
              ┌─────────────────────────────────┼─────────────────────────────────┐
              │                                 │                                 │
              ▼                                 ▼                                 ▼
   ┌─────────────────────┐          ┌─────────────────────┐          ┌─────────────────────┐
   │   /api/v1/query     │          │  /api/v1/query/stream │          │    /api/v1/upload   │
   │   同步查询入口      │          │   SSE流式查询入口     │          │    文档上传入口      │
   └──────────┬──────────┘          └──────────┬──────────┘          └──────────┬──────────┘
              │                                 │                                 │
              └─────────────────────────────────┼─────────────────────────────────┘
                                                │
                     ┌───────────────────────────┼───────────────────────────┐
                     │                           │                           │
                     ▼                           ▼                           ▼
          ┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐
          │  get_agent依赖   │      │  get_agent依赖    │      │  get_vectorstore  │
          │  验证API Key     │      │  验证API Key      │      │     依赖注入       │
          └────────┬─────────┘      └────────┬─────────┘      └────────┬─────────┘
                   │                         │                         │
                   └────────────┬────────────┘                         │
                                │                                    │
                                ▼                                    │
                     ┌─────────────────────────┐                      │
                     │   AgenticRAGGraph      │                      │
                     │  ┌─────────────────┐   │                      │
                     │  │ use_fast_path?  │───┼───► fast_stream_invoke│
                     │  │ (默认True)      │   │     手动执行+真流式   │
                     │  └─────────────────┘   │     首token<2s       │
                     │        │              │                       │
                     │        ▼              │                       │
                     │  ┌─────────────────┐  │                       │
                     │  │ use_react=True? │──┼───► ReActAgent       │
                     │  └─────────────────┘  │     Observe→Think→Act │
                     │        │              │                       │
                     │        ▼              │                       │
                     │   LangGraph StateGraph│                       │
                     │   (标准DAG路径)       │                       │
                     └─────────────────────┬─────────────────────────┘
                                           │
              ┌────────────────────────────┼────────────────────────────┐
              │                            │                            │
              ▼                            ▼                            ▼
   ┌──────────────────┐       ┌──────────────────┐        ┌──────────────────┐
   │  ShortTermMemory │       │  LongTermMemory  │        │    VectorStore   │
   │  (PostgreSQL)    │       │      V2          │        │    (Milvus)      │
   │  - 会话隔离       │       │  - 价值筛选      │        │  - 向量检索      │
   │  - 24h TTL       │       │  - 四类型分类    │        │  - 单例模式      │
   │  - Token截断      │       │  - 时间衰减      │        │                  │
   └──────────────────┘       └──────────────────┘        └──────────────────┘
```

---

## 十、数据流总览

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│                                    用户请求                                         │
│                                         │                                          │
│                                         ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────────────┐      │
│  │                              API 网关                                     │      │
│  │  限流(60req/min) → 追踪ID → API Key验证 → CORS → 全局异常处理           │      │
│  └─────────────────────────────────────────────────────────────────────────┘      │
│                                         │                                          │
│                                         ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────────────┐      │
│  │                     Agent 执行层 (四路径路由)                             │      │
│  │                                                                             │
│  │  ┌──────────────────────────────────────────────────────────────┐        │      │
│  │  │ 路径1: ultra_fast_stream_invoke (终极并行, use_fast_path=True)│        │      │
│  │  │ ① 意图+改写 → ParallelExecutor三路并行 → 真流式生成          │        │      │
│  │  │ Memory‖Retrieval‖WebSearch并行，首token < 2-3s                │        │      │
│  │  └──────────────────────────────────────────────────────────────┘        │      │
│  │                                                                             │
│  │  ┌──────────────────────────────────────────────────────────────┐        │      │
│  │  │ 路径2: fast_stream_invoke (快速流式, use_fast_path=True)      │        │      │
│  │  │ ① 并行加载记忆 → 意图+改写 → 检索+重排 → 真流式生成          │        │      │
│  │  │ Memory串行等待，首token < 3-5s                                │        │      │
│  │  └──────────────────────────────────────────────────────────────┘        │      │
│  │                                                                             │
│  │  ┌──────────────────────────────────────────────────────────────┐        │      │
│  │  │ 路径3: graph.astream() (标准DAG, use_fast_path=False)        │        │      │
│  │  │ intent→retrieval→rerank→generation→evaluation→CRAG→reflection│        │      │
│  │  └──────────────────────────────────────────────────────────────┘        │      │
│  │                                                                             │
│  │  ┌──────────────────────────────────────────────────────────────┐        │      │
│  │  │ 路径4: ReAct stream_run (mode="react")                       │        │      │
│  │  │ Observe→Think→Act循环 + llm.astream()真流式                  │        │      │
│  │  └──────────────────────────────────────────────────────────────┘        │      │
│  └─────────────────────────────────────────────────────────────────────────┘      │
│                                         │                                          │
│                                         ▼                                          │
│                               响应用户 (SSE/JSON)                                  │
└──────────────────────────────────────────────────────────────────────────────────┘
```

---

## 十一、设计思想总结

### 核心理念
1. **用户感知速度优先**：先给用户一个答案，比给用户一个完美答案更重要
2. **四路径智能路由**：超快速并行（默认）→ 快速流式 → 标准DAG（精确）→ ReAct（复杂）
3. **最大化并行执行**：Memory+Retrieval+WebSearch 三路并行，不等待无依赖任务
4. **多级检索增强**：规则预过滤 → 向量检索 → BGE重排 → CRAG兜底
5. **价值驱动的记忆**：长期记忆不追求全量存储，追求高价值密度
6. **后台异步不阻塞**：评估/记忆保存/CRAG日志全部后台执行

### 架构亮点
- `ultra_fast_stream_invoke`通过`ParallelExecutor`实现Memory+Retrieval+WebSearch三路并行
- `fast_stream_invoke`绕过graph直接用`llm.astream()`实现真流式
- 单例模式减少向量库连接开销
- 意图缓存减少LLM调用
- 分层检索综合sim/value/decay三维度
- CRAG循环守卫防止无限搜索
- 后台`_background_post_processing`不阻塞用户
- `asyncio.to_thread()`包装同步调用，不阻塞事件循环
- **生成缓存双键写入**：`set()`时同时写入`(question,intent)`和`(question,None)`两个键，`get()`时优先精确匹配，回退兼容老缓存，命中率提升50%+

### 待改进方向
- ~~统一缓存策略设计~~（已完成：双键写入+回退兼容）
- 减少全局隐式状态
- 简化双模式维护成本
- Speculative Decoding进一步降低首token时间

---

## 十二、缺失模块补充（基于代码实际结构）

### 12.1 分布式锁模块（agentic_rag/lock/）

**RedisDistributedLock**：
- 基于 `SET NX EX` 原子操作
- Lua脚本保证释放锁原子性
- 支持看门狗续期（extend方法）
- 使用场景：文档索引去重、缓存更新防击穿、定时任务互斥

**PostgresLockManager**：
- 行级锁：`SELECT FOR UPDATE` 防止并发修改同一会话
- 咨询锁：`pg_advisory_lock` 用于跨实例协调
- 上下文管理器确保锁生命周期一致

### 12.2 评估模块（agentic_rag/evaluation/）

**RAGEvaluator**：
- 忠实度评估（Faithfulness）：检测幻觉
- 答案相关性（Relevancy）：评估回答是否切题
- 上下文精确度（Context Precision）：关键词重叠算法
- 完整性评分（Completeness）：LLM判断覆盖度
- 批量评估支持：asyncio.Semaphore 限制并发数

### 12.3 文档处理模块（agentic_rag/document_processing/）

**DocumentLoader**：支持9种格式（PDF/Word/Markdown/TXT/CSV/Excel/HTML）
**TextSplitter**：3种分块策略
- RecursiveChunker：递归字符分割
- SemanticChunker：基于spaCy分句/段落
- AdaptiveChunker：根据文档类型自适应调整

### 12.4 混合检索（agentic_rag/retrieval/hybrid_search.py）

**HybridSearchRetriever**：
- 向量检索（权重0.6）+ BM25关键词检索（权重0.4）
- 预计算文档向量，避免重复计算
- 分数归一化后加权融合

### 12.5 记忆子模块

**MemoryCompressor**：
- LLM提取高价值记忆候选
- 经验泛化：具体事件→通用规则
- 过度压缩检测：防止信息丢失

**MemoryEvaluator**：
- 四维评分公式：`score = 0.2*P + 0.25*R + 0.25*U + 0.3*D`
  - P（持久性）：信息衰减速度
  - R（可复用性）：跨场景适用性
  - U（独特性）：外部不可获取性
  - D（决策影响力）：缺失导致错误决策的风险
- 时间衰减：不同类型记忆有不同半衰期（user_profile:365天, fact:180天, experience:90天, preference:60天）

---

## 十三、API端点文档

### 13.1 API列表

| 端点 | 方法 | 功能 | 认证 |
|------|------|------|------|
| `/api/v1/query` | POST | 同步RAG查询 | API Key |
| `/api/v1/query/stream` | POST | SSE流式查询 | API Key |
| `/api/v1/upload` | POST | 文档上传索引 | API Key |
| `/api/v1/health` | GET | 健康检查 | 无 |
| `/api/docs` | GET | OpenAPI文档 | 无 |

### 13.2 请求/响应格式

**QueryRequest**：
```json
{
  "question": "string (1-2000字符)",
  "session_id": "string (可选, 字母数字下划线)",
  "user_id": "string (可选)",
  "model_name": "string (可选: qwen3.5-plus, MiniMax-M2.6, glm-4等)",
  "use_tools": true,
  "use_fast_path": true,
  "max_reflection": 2,
  "temperature": 0.7,
  "mode": "react | dag (可选)"
}
```

**QueryResponse**：
```json
{
  "answer": "AI回答内容",
  "sources": [{"content": "...", "metadata": {}, "score": 0.9, "source": "file.pdf"}],
  "metrics": {"faithfulness": 0.8, "answer_relevancy": 0.9, ...},
  "session_id": "session-123",
  "intent": "technical",
  "tools_used": ["duckduckgo"],
  "reflection_count": 0,
  "processing_time": 2.5
}
```

### 13.3 认证机制
- 请求头：`X-API-Key: <your_api_key>`
- 配置来源：`.env` 文件的 `API_KEY` 变量
- 失败响应：401（缺少Key）/ 403（无效Key）

---

## 十四、数据模型设计

### 14.1 短期记忆表（PostgreSQL）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| session_id | VARCHAR | 会话ID |
| message_index | INTEGER | 消息序号 |
| role | VARCHAR | 角色(user/assistant) |
| content | TEXT | 消息内容 |
| created_at | TIMESTAMP | 创建时间 |
| ttl | TIMESTAMP | 过期时间（24h后） |

### 14.2 长期记忆V2表（PostgreSQL + pgvector）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| user_id | VARCHAR | 用户ID |
| content | TEXT | 记忆内容 |
| memory_type | VARCHAR | user_profile/fact/experience/preference |
| summary | VARCHAR | 一句话摘要 |
| value_score | FLOAT | 价值分数(0-1) |
| embedding | VECTOR(1024) | 语义向量 |
| access_count | INTEGER | 被检索次数 |
| created_at | TIMESTAMP | 创建时间 |

### 14.3 AgentState 状态字段
```
question, intent, retrieved_docs, reranked_docs,
generation, refined_answer, evaluation, memory_context,
conversation_history, crag_loop_count, metadata
```

---

## 十五、错误处理与降级策略

### 15.1 全局异常处理
- 所有异常统一捕获，生成 `request_id` 追踪
- 返回友好错误响应（`ErrorResponse` 模型）
- 详细堆栈记录到 Loguru

### 15.2 降级策略
| 场景 | 降级行为 |
|------|---------|
| LLM调用失败 | 返回默认分数0.5（评估模块） |
| 缓存未命中 | 回源到实际向量检索 |
| 向量库未初始化 | 503错误提示 |
| Agent未初始化 | 503错误提示 |
| 记忆模块初始化失败 | 跳过记忆加载，继续运行 |
| 文档解析失败 | 记录日志，跳过该文件 |
| CRAG评估失败 | 使用默认置信度，不触发额外搜索 |
| fast_stream失败 | 自动回退到标准DAG路径 |

### 15.3 限流策略
- 滑动窗口算法：60请求/分钟/IP
- 每5分钟清理过期IP记录，防止内存泄漏
- 超限返回 429 状态码

---

## 十六、运维与部署

### 16.1 环境配置
- 配置文件：`.env.example` 提供模板
- 加载方式：`pydantic-settings` 自动加载 `.env`
- 关键变量：`QWEN_API_KEY`, `DATABASE_URL`, `API_KEY` 等

### 16.2 日志策略
- 使用 Loguru 结构化日志
- 每个请求生成唯一 `X-Request-ID`
- 记录处理时间 `X-Process-Time`
- 日志级别可配置（`log_level` 配置项）

### 16.3 定时任务
- `TaskScheduler`（APScheduler）：定期清理过期记忆数据
- 分布式锁保护：防止多实例重复执行

### 16.4 健康检查
- `/api/v1/health` 端点检查所有组件状态
- 检查项：database、vectorstore、agent
- 总体状态：healthy / degraded / unhealthy

### 16.5 动态模型切换
- 请求级指定 `model_name` 参数
- 支持模型：Qwen3.5/3.6-Plus, MiniMax-M2.6/2.7, GLM-4
- 加锁防止并发竞态（`threading.Lock`）

---

## 十七、测试覆盖

### 17.1 测试文件列表
| 文件 | 测试范围 |
|------|---------|
| `test_api.py` | API路由、限流、认证 |
| `test_nodes.py` | DAG节点逻辑 |
| `test_edges.py` | 状态机边路由 |
| `test_crag.py` | CRAG置信度路由 |
| `test_memory.py` | 短期/长期记忆 |
| `test_caches.py` | 意图/生成缓存 |
| `test_retrieval.py` | 向量检索+重排 |
| `test_tools.py` | 外部工具调用 |
| `test_locks.py` | 分布式锁 |
| `test_evaluation.py` | 评估模块 |
| `test_integration.py` | 端到端集成 |
| `test_stream_api.py` | SSE流式输出 |

### 17.2 运行测试
```bash
cd agentic_rag/tests
python run_all_tests.py
```

---

## 十八、配置参数总览

### 18.1 LLM配置
```
llm_name: qwen3.5-plus (默认)
llm_temperature: 0.7
llm_max_tokens: 4096
```

### 18.2 检索配置
```
retrieval_top_k: 5
rerank_top_k: 3
similarity_threshold: 0.5
```

### 18.3 记忆配置
```
short_term_memory_k: 10
long_term_memory_k: 3
long_term_memory_max_per_user: 500
```

### 18.4 CRAG配置
```
crag_enabled: true
crag_confidence_threshold_high: 0.7
crag_confidence_threshold_low: 0.3
```

### 18.5 缓存配置
```
intent_cache_max_size: 200, TTL: 1800s
generation_cache_max_size: 100, TTL: 3600s
```

### 18.6 上下文截断
```
context_truncation_enabled: true
max_context_tokens: 8000
max_docs_for_context: 5
```

### 18.7 快速流式配置（新增）
```
fast_stream_enabled: true
fast_stream_default: true
```

### 18.8 并行执行器配置（新增）
```
parallel_executor_enabled: true
parallel_max_workers: 5  # ThreadPoolExecutor 最大线程数
parallel_enable_web_search: false  # Web Search 默认不参与并行
parallel_deduplicate_docs: true  # 跨查询去重
```

### 18.9 Agent 执行模式配置
```
agent_mode: dag  # 默认DAG模式（快速流式），复杂问题自动切换ReAct
```
